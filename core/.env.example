# core 运行时配置。
PORT=8788
HOST=127.0.0.1
LOG_LEVEL=info
CORS_ORIGINS=http://localhost:5173,tauri://localhost
# LLM 提供方：mock（回显，用于联调）/ openai-compatible（v2 实现）。
LLM_PROVIDER=mock
LLM_MODEL=gpt-4o-mini
# 仅 openai-compatible provider 需要：
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_API_KEY=sk-...
