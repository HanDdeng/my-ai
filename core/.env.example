# core 运行时配置。
PORT=8788
HOST=127.0.0.1
LOG_LEVEL=info
CORS_ORIGINS=http://localhost:5173,tauri://localhost
# LLM API key（openai-compatible provider 需要；留空 = mock 走回显）
# LLM_API_KEY=sk-...
# SQLite 文件路径（相对路径相对 cwd）；默认 ./core.db；测试可用 :memory:
CORE_DB_PATH=./core.db
