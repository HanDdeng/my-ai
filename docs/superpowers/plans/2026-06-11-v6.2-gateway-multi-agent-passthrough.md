# v6.2 Gateway Multi-Agent Passthrough Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 gateway 端加 9 个新透传端点（POST /v1/agents / GET-PATCH-DELETE /v1/agents/{id} / POST /v1/sessions / GET-DELETE /v1/sessions/{id} / GET-POST /v1/sessions/{id}/messages）+ 改造 GET /v1/agents 走新 `CoreClient.call()` 辅助 + 内部 `X-Internal-Client-Key: <sha256 hash>` header 注入 + 9 错码整包透传 + 双层测试（路由单测 + integration）。

**Architecture:** gateway 端零 DB 改动（DB 在 v6.1 core 端已就绪）；纯透传层 —— 路由 handler 调 `CoreClient` 11 个方法之一，`CoreClient.call()` 内部统一挂内部头 + 走 undici 发请求 + 解析响应外层 `{data, code, message}` 仅取 `.data` 字段 + 整包回包。鉴权沿用 v3 现状（authMiddleware 不动），`req.clientCtx.id`（事实为 sha256 hash）作内部头源。错误码透传：core 4xx/5xx 整包透传；仅 gateway 网络层异常（core 不可达 / 超时）由 handler catch → 502 `upstream_error`。

**Tech Stack:** Node 20+ / Fastify 4 / TypeScript 5 / vitest 2 / undici 6 / pino。沿用 v3 现状测试风格：`app.inject()` + `openDatabase(':memory:')` + `app.decorate('core', mockInstance)`（不引入 vi.mock）。

---

## File Structure

### 新增

- `gateway/src/routes/sessions.ts` —— 3 端点（POST / GET-id / DELETE）
- `gateway/src/routes/messages.ts` —— 2 端点（GET / POST）
- `gateway/test/routes/sessions.test.ts` —— sessions 路由单测
- `gateway/test/routes/messages.test.ts` —— messages 路由单测
- `gateway/test/integration/gateway-core.test.ts` —— 端到端 integration（mock core HTTP server + 真实 buildServer + 真实 AuthStore + 跨 clientKey 共享 + 头注入断言）

### 修改

- `gateway/src/clients/core.ts` —— 加 `private call()` 辅助；`listAgents` 改签名 `(clientKey)`；加 9 新方法（createAgent / getAgent / updateAgent / deleteAgent / createSession / getSession / deleteSession / listMessages / postMessage）
- `gateway/src/routes/agents.ts` —— 扩 4 端点（POST / GET-id / PATCH / DELETE）；GET 改造走 call()；每个 handler 调 `getClientKey(req)`
- `gateway/src/server.ts` —— 在 `await app.register(async instance => { await agentRoutes(...) })` 块内增 `await sessionRoutes / messagesRoutes`
- `gateway/test/clients/core.test.ts` —— 改 `listAgents` 测试（新增 clientKey 入参）；加 9 新方法测试

### 不修改

- v3 现状的 `gateway/src/auth/*`（零改动；clientKey 语义=hash 决策已与 v3 现状对齐）
- v3 现状的 `gateway/src/config.ts` / `gateway/src/db.ts` / `gateway/src/cli.ts` / `gateway/src/index.ts` / `gateway/src/logger.ts` / `gateway/src/compat/*` / `gateway/src/response.ts`
- v3 现状的 `gateway/src/routes/health.ts` / `pair.ts` / `pair-status.ts` / `internal/*`
- core 端所有文件（v6.1 已实现，v6.2 不动）
- client 端所有文件（v6.2 不动）

---

## Task 0: 准备（preflight）

**Files:**

- Read: `gateway/src/clients/core.ts` / `gateway/src/routes/agents.ts` / `gateway/src/server.ts` / `gateway/src/auth/middleware.ts`
- Run: `cd /home/handdeng/rd-center/my-ai && git status` + `git branch --show-current` + `pnpm -C gateway test` 验证基线全绿

- [ ] **Step 1: 确认当前在 dev-20260610 或新建 dev-20260611 分支**

Run: `cd /home/handdeng/rd-center/my-ai && git status`
Expected: clean working tree on branch `dev-20260610`（v6.1 现状）

若需新建分支（按 CLAUDE.md "开发分支命名规范 dev-YYYYMMDD，时间为当日"——今日 2026-06-11）：

```bash
cd /home/handdeng/rd-center/my-ai
git checkout -b dev-20260611
```

- [ ] **Step 2: 跑基线测试确认 v3 现状全绿**

Run: `pnpm -C gateway test 2>&1 | tail -30`
Expected: `Test Files  6+ passed (6+)` / `Tests  20+ passed (20+)` —— v3 现状全绿

- [ ] **Step 3: 跑基线 typecheck**

Run: `pnpm -C gateway typecheck 2>&1 | tail -10`
Expected: 无错误输出（exit 0）

- [ ] **Step 4: 读关键基线文件**

读：`gateway/src/clients/core.ts` / `gateway/src/routes/agents.ts` / `gateway/src/server.ts` / `gateway/src/auth/middleware.ts` / `gateway/src/response.ts` / `gateway/test/clients/core.test.ts` / `gateway/test/routes/internal/clients.test.ts`

确认理解：

- `req.clientCtx.id` 字段（v3 middleware 挂，事实为 sha256 hash）
- `ok(data)` / `err(code, message)` 包装函数
- `AuthStore` 用 `:memory:` SQLite 在 test 里
- 路由测试用 `Fastify({ logger: false })` + `app.inject()` 风格

---

## Task 1: CoreClient 加 private call() 辅助（最基础模块）

**Files:**

- Modify: `gateway/src/clients/core.ts`
- Test: `gateway/test/clients/core.test.ts`

- [ ] **Step 1: 写失败测试 —— call() 透传 core 响应（status + data）**

把现有 `gateway/test/clients/core.test.ts` 内容替换为：

```ts
// CoreClient 单元测试：覆盖 listAgents 透传 + 9 个新业务方法 + 错误码矩阵。
// 集成测走 task 10；本文件仅测 CoreClient 类与 mock core server 的契约。
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { createServer, type Server, type IncomingMessage, type ServerResponse } from 'node:http';
import { CoreClient } from '@/clients/core.js';

let server: Server;
let baseUrl: string;
let lastRequest: {
  method?: string;
  url?: string;
  headers: Record<string, string | string[] | undefined>;
  body?: string;
};

beforeAll(async () => {
  server = createServer((req: IncomingMessage, res: ServerResponse) => {
    // 收集请求元数据供测试断言
    const chunks: Buffer[] = [];
    req.on('data', c => chunks.push(c));
    req.on('end', () => {
      lastRequest = {
        method: req.method,
        url: req.url,
        headers: req.headers,
        body: chunks.length > 0 ? Buffer.concat(chunks).toString('utf8') : undefined,
      };

      // 简单 mock：根据 url 路径回包
      if (req.url === '/v1/agents' && req.method === 'GET') {
        res.statusCode = 200;
        res.setHeader('Content-Type', 'application/json');
        res.end(JSON.stringify({ data: [{ id: 'a1', name: 'Echo' }], code: 0, message: 'ok' }));
        return;
      }
      if (req.url?.startsWith('/v1/agents/') && req.method === 'GET') {
        res.statusCode = 200;
        res.setHeader('Content-Type', 'application/json');
        res.end(JSON.stringify({ data: { id: 'a1', name: 'Echo' }, code: 0, message: 'ok' }));
        return;
      }
      // 默认 404
      res.statusCode = 404;
      res.setHeader('Content-Type', 'application/json');
      res.end(JSON.stringify({ data: null, code: 404, message: 'not_found' }));
    });
  });
  await new Promise<void>(resolve => server.listen(0, '127.0.0.1', resolve));
  const addr = server.address();
  if (typeof addr === 'string' || addr === null) throw new Error('unexpected address');
  baseUrl = `http://127.0.0.1:${addr.port}`;
});

afterAll(async () => {
  await new Promise<void>((resolve, reject) =>
    server.close(err => (err ? reject(err) : resolve())),
  );
});

describe('CoreClient.call()', () => {
  it('GET /v1/agents 透传 status + data', async () => {
    const c = new CoreClient({ baseUrl });
    const result = await c.listAgents('hash-abc');
    expect(result.status).toBe(200);
    expect(result.data).toEqual([{ id: 'a1', name: 'Echo' }]);
  });

  it('挂 X-Internal-Client-Key 头（值 = 传入 clientKey）', async () => {
    const c = new CoreClient({ baseUrl });
    await c.listAgents('sha256-xyz');
    expect(lastRequest.headers['x-internal-client-key']).toBe('sha256-xyz');
  });

  it('GET /v1/agents/{id} 透传 id（url encoded）', async () => {
    const c = new CoreClient({ baseUrl });
    const result = await c.getAgent('hash', 'a1');
    expect(result.status).toBe(200);
    expect(result.data).toEqual({ id: 'a1', name: 'Echo' });
    expect(lastRequest.url).toBe('/v1/agents/a1');
  });

  it('core 4xx 整包透传（status 原样 + data 为 null）', async () => {
    // 临时替换 server 行为
    server.removeAllListeners('request');
    server.on('request', (_req, res) => {
      res.statusCode = 404;
      res.setHeader('Content-Type', 'application/json');
      res.end(JSON.stringify({ data: null, code: 404, message: 'agent_not_found' }));
    });
    const c = new CoreClient({ baseUrl });
    const result = await c.getAgent('hash', 'nonexistent');
    expect(result.status).toBe(404);
    expect(result.data).toBeNull();
    // 恢复默认 server 行为
    server.removeAllListeners('request');
    server.on('request', (req: IncomingMessage, res: ServerResponse) => {
      const chunks: Buffer[] = [];
      req.on('data', c => chunks.push(c));
      req.on('end', () => {
        if (req.url === '/v1/agents' && req.method === 'GET') {
          res.statusCode = 200;
          res.setHeader('Content-Type', 'application/json');
          res.end(JSON.stringify({ data: [{ id: 'a1', name: 'Echo' }], code: 0, message: 'ok' }));
          return;
        }
        if (req.url?.startsWith('/v1/agents/') && req.method === 'GET') {
          res.statusCode = 200;
          res.setHeader('Content-Type', 'application/json');
          res.end(JSON.stringify({ data: { id: 'a1', name: 'Echo' }, code: 0, message: 'ok' }));
          return;
        }
        res.statusCode = 404;
        res.end();
      });
    });
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm -C gateway test test/clients/core.test.ts 2>&1 | tail -30`
Expected: FAIL with "c.listAgents is not a function" / "c.getAgent is not a function"（因为 core.ts 还没扩方法）

- [ ] **Step 3: 写实现 —— 扩 `core.ts` 加 call() + listAgents / getAgent**

替换 `gateway/src/clients/core.ts` 完整内容为：

```ts
// 上游 core 的 HTTP 客户端：基于 undici，比 Node 内置 fetch 更轻、控时更细。
// v6.2：扩 9 个方法 + 统一 call() 辅助挂 X-Internal-Client-Key 头。
// clientKey 入参 = sha256 hash（v3 middleware req.clientCtx.id 字段；不做重哈希）。
import { request } from 'undici';

export type CoreClientOptions = {
  baseUrl: string;
  timeoutMs?: number;
};

export class CoreClient {
  private readonly baseUrl: string;
  private readonly timeoutMs: number;

  constructor(opts: CoreClientOptions) {
    this.baseUrl = opts.baseUrl.replace(/\/+$/, '');
    this.timeoutMs = opts.timeoutMs ?? 15_000;
  }

  /**
   * 统一调用入口：挂 X-Internal-Client-Key 头 + 处理 HTTP 状态 / body 透传。
   * - 2xx：返回 { status, data: <.data 字段或 null> }
   * - 4xx/5xx：原 status 透传 + data = <.data 字段>（gateway handler 整包回包）
   * - 网络错：throw Error（handler catch → 502 upstream_error）
   */
  private async call(
    method: 'GET' | 'POST' | 'PATCH' | 'DELETE',
    path: string,
    clientKey: string,
    body?: unknown,
  ): Promise<{ status: number; data: unknown }> {
    const res = await request(`${this.baseUrl}${path}`, {
      method,
      headers: {
        'Content-Type': 'application/json',
        'X-Internal-Client-Key': clientKey,
      },
      headersTimeout: this.timeoutMs,
      bodyTimeout: this.timeoutMs,
      body: body !== undefined ? JSON.stringify(body) : undefined,
    });

    const status = res.statusCode;
    if (status === 204) return { status, data: null };
    const json = (await res.body.json()) as { data?: unknown };
    return { status, data: json.data ?? null };
  }

  /**
   * 上游健康检查：5xx 视为 down，避免 core 启动中导致网关"假绿"。
   * v6.2 保留 v3 现状签名（不挂内部头；/health 是白名单）。
   */
  async health(): Promise<{ ok: boolean; service: string }> {
    const res = await request(`${this.baseUrl}/health`, {
      method: 'GET',
      headersTimeout: this.timeoutMs,
      bodyTimeout: this.timeoutMs,
    });
    if (res.statusCode >= 500) {
      return { ok: false, service: 'core' };
    }
    return (await res.body.json()) as { ok: boolean; service: string };
  }

  // === agents（v3 现状 listAgents 改造 + 4 新 CRUD 方法）===

  async listAgents(clientKey: string): Promise<{ status: number; data: unknown[] }> {
    return this.call('GET', '/v1/agents', clientKey) as Promise<{
      status: number;
      data: unknown[];
    }>;
  }

  async createAgent(clientKey: string, body: unknown): Promise<{ status: number; data: unknown }> {
    return this.call('POST', '/v1/agents', clientKey, body);
  }

  async getAgent(clientKey: string, id: string): Promise<{ status: number; data: unknown }> {
    return this.call('GET', `/v1/agents/${encodeURIComponent(id)}`, clientKey);
  }

  async updateAgent(
    clientKey: string,
    id: string,
    body: unknown,
  ): Promise<{ status: number; data: unknown }> {
    return this.call('PATCH', `/v1/agents/${encodeURIComponent(id)}`, clientKey, body);
  }

  async deleteAgent(clientKey: string, id: string): Promise<{ status: number; data: unknown }> {
    return this.call('DELETE', `/v1/agents/${encodeURIComponent(id)}`, clientKey);
  }

  // === sessions（3 新方法）===

  async createSession(
    clientKey: string,
    body: unknown,
  ): Promise<{ status: number; data: unknown }> {
    return this.call('POST', '/v1/sessions', clientKey, body);
  }

  async getSession(clientKey: string, id: string): Promise<{ status: number; data: unknown }> {
    return this.call('GET', `/v1/sessions/${encodeURIComponent(id)}`, clientKey);
  }

  async deleteSession(clientKey: string, id: string): Promise<{ status: number; data: unknown }> {
    return this.call('DELETE', `/v1/sessions/${encodeURIComponent(id)}`, clientKey);
  }

  // === messages（2 新方法）===

  async listMessages(
    clientKey: string,
    sessionId: string,
  ): Promise<{ status: number; data: unknown }> {
    return this.call('GET', `/v1/sessions/${encodeURIComponent(sessionId)}/messages`, clientKey);
  }

  async postMessage(
    clientKey: string,
    sessionId: string,
    body: unknown,
  ): Promise<{ status: number; data: unknown }> {
    return this.call(
      'POST',
      `/v1/sessions/${encodeURIComponent(sessionId)}/messages`,
      clientKey,
      body,
    );
  }
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm -C gateway test test/clients/core.test.ts 2>&1 | tail -20`
Expected: PASS（4 个 it 全过）

- [ ] **Step 5: 跑 typecheck 验证无错**

Run: `pnpm -C gateway typecheck 2>&1 | tail -10`
Expected: 无错误输出（exit 0）

- [ ] **Step 6: 提交**

```bash
cd /home/handdeng/rd-center/my-ai
git add gateway/src/clients/core.ts gateway/test/clients/core.test.ts
git commit -m "v6.2: extend CoreClient with 9 business methods + private call() helper"
```

注：CLAUDE.md 约定 git author 字段已由 git config 默认设置（handdeng），不加 Co-Authored-By: Claude trailer。

---

## Task 2: agents.ts 扩 4 端点（POST / GET-id / PATCH / DELETE）+ GET 改造

**Files:**

- Modify: `gateway/src/routes/agents.ts`
- Test: `gateway/test/routes/agents.test.ts`（v3 现状扩 4 端点 case + 错误码矩阵）

- [ ] **Step 1: 写失败测试 —— POST /v1/agents + GET-id / PATCH / DELETE + 错误码透传**

新建 `gateway/test/routes/agents.test.ts`（v3 现状无此文件，v6.2 新建）：

```ts
// /v1/agents 路由层单测：5 端点（GET 列表 / POST / GET-id / PATCH / DELETE）+ 错误码透传矩阵。
// 用 app.decorate('core', mockCore) 注入假 CoreClient；不真实发请求。
import { describe, it, expect, beforeEach } from 'vitest';
import Fastify, { type FastifyInstance } from 'fastify';
import { openDatabase } from '@/db.js';
import { AuthStore } from '@/auth/store.js';
import { authMiddleware } from '@/auth/middleware.js';
import { agentRoutes } from '@/routes/agents.js';
import type { CoreClient } from '@/clients/core.js';

type CoreResponse = { status: number; data: unknown };

function buildMockCore(responses: Record<string, CoreResponse> = {}): CoreClient {
  return {
    listAgents: vi(async (_ck: string) => responses.listAgents ?? { status: 200, data: [] }),
    createAgent: vi(
      async (_ck: string, _body: unknown) =>
        responses.createAgent ?? { status: 200, data: { id: 'new' } },
    ),
    getAgent: vi(
      async (_ck: string, _id: string) => responses.getAgent ?? { status: 200, data: { id: 'a1' } },
    ),
    updateAgent: vi(
      async (_ck: string, _id: string, _body: unknown) =>
        responses.updateAgent ?? { status: 200, data: { id: 'a1' } },
    ),
    deleteAgent: vi(
      async (_ck: string, _id: string) => responses.deleteAgent ?? { status: 200, data: null },
    ),
    createSession: vi(async () => ({ status: 200, data: null })),
    getSession: vi(async () => ({ status: 200, data: null })),
    deleteSession: vi(async () => ({ status: 200, data: null })),
    listMessages: vi(async () => ({ status: 200, data: [] })),
    postMessage: vi(async () => ({ status: 200, data: null })),
    health: vi(async () => ({ ok: true, service: 'core' })),
  } as unknown as CoreClient;
}

async function buildApp(
  core: CoreClient,
): Promise<{ app: FastifyInstance; store: AuthStore; ckHeader: string }> {
  const store = new AuthStore(openDatabase(':memory:'));
  // v3 现状事实：id 字段就是 sha256 hash（auth/store.ts:92-96）
  const ck = 'test-sha256-hash';
  store.insertClient({
    id: ck,
    keyHash: ck,
    name: 'tester',
    createdAt: Date.now(),
    lastSeenAt: Date.now(),
  });

  const app = Fastify({ logger: false });
  app.decorate('core', core);
  app.decorate('authStore', store);
  await app.register(authMiddleware);
  await app.register(async instance => {
    await agentRoutes(instance, core);
  });
  return { app, store, ckHeader: ck };
}

describe('/v1/agents routes', () => {
  it('GET /v1/agents：200 + 透传 core.data', async () => {
    const core = buildMockCore({ listAgents: { status: 200, data: [{ id: 'a1' }] } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/agents',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: [{ id: 'a1' }], code: 0, message: 'ok' });
  });

  it('POST /v1/agents：200 + 透传 core.data', async () => {
    const core = buildMockCore({
      createAgent: { status: 200, data: { id: 'a-new', name: 'New' } },
    });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'POST',
      url: '/v1/agents',
      headers: { 'x-client-key': ckHeader, 'content-type': 'application/json' },
      payload: { id: 'a-new', name: 'New' },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: { id: 'a-new', name: 'New' }, code: 0, message: 'ok' });
  });

  it('GET /v1/agents/{id}：200 + 透传 core.data', async () => {
    const core = buildMockCore({ getAgent: { status: 200, data: { id: 'a1', name: 'Echo' } } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/agents/a1',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: { id: 'a1', name: 'Echo' }, code: 0, message: 'ok' });
  });

  it('PATCH /v1/agents/{id}：200 + 透传 core.data', async () => {
    const core = buildMockCore({
      updateAgent: { status: 200, data: { id: 'a1', name: 'Renamed' } },
    });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'PATCH',
      url: '/v1/agents/a1',
      headers: { 'x-client-key': ckHeader, 'content-type': 'application/json' },
      payload: { name: 'Renamed' },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: { id: 'a1', name: 'Renamed' }, code: 0, message: 'ok' });
  });

  it('DELETE /v1/agents/{id}：200 + 透传 core.data', async () => {
    const core = buildMockCore({ deleteAgent: { status: 200, data: null } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'DELETE',
      url: '/v1/agents/a1',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: null, code: 0, message: 'ok' });
  });

  it('错误码透传：core 4xx 整包透传（status + data）', async () => {
    const core = buildMockCore({ getAgent: { status: 404, data: null } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/agents/missing',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(404);
    expect(res.json().data).toBeNull();
  });

  it('错误码透传：core 5xx 整包透传', async () => {
    const core = buildMockCore({ getAgent: { status: 502, data: null } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/agents/missing',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(502);
  });

  it('gateway 网络层异常 → 502 upstream_error', async () => {
    const core = {
      getAgent: vi(async () => {
        throw new Error('ECONNREFUSED');
      }),
    } as unknown as CoreClient;
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/agents/a1',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(502);
    expect(res.json()).toEqual({ data: null, code: 502, message: 'upstream_error' });
  });

  it('鉴权失败：无 X-Client-Key → 401 missing_key，**不**调 core', async () => {
    const core = buildMockCore();
    const { app } = await buildApp(core);
    const res = await app.inject({ method: 'GET', url: '/v1/agents' });
    expect(res.statusCode).toBe(401);
    expect(res.json()).toEqual({ data: null, code: 401, message: 'missing_key' });
  });
});
```

注：测试用 vitest 的 `vi` 全局 mock（与 v3 现状的 test/internal/clients.test.ts 风格一致——它直接构造 mock 实例而不引入 vi.mock）；上面用了 `vi(...)` 包装断言式 mock fn，确保调用次数 / 参数可断言。验证 `vi` 在 vitest 上下文可用——它在 describe/it 内是 vitest 全局提供的。

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm -C gateway test test/routes/agents.test.ts 2>&1 | tail -30`
Expected: FAIL —— 多个 it 失败，因为 `agents.ts` 还没扩 4 端点

- [ ] **Step 3: 写实现 —— 扩 agents.ts 5 端点**

替换 `gateway/src/routes/agents.ts` 完整内容为：

```ts
// gateway 透传 /v1/agents 到 core；v3 起走新响应包装，v6.2 扩 4 端点（POST/GET-id/PATCH/DELETE）。
// 错误码透传：core 4xx/5xx 整包透传（status + data 字段）；gateway 网络层异常 → 502 upstream_error。
import type { FastifyInstance, FastifyRequest } from 'fastify';
import { ok, err } from '../response.js';
import type { CoreClient } from '../clients/core.js';

function getClientKey(req: FastifyRequest): string {
  if (!req.clientCtx) throw new Error('clientCtx not set (auth middleware not run?)');
  return req.clientCtx.id;
}

export async function agentRoutes(app: FastifyInstance, core: CoreClient) {
  // GET /v1/agents（v3 现状路径；v6.2 改造走 call()）
  app.get('/v1/agents', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.listAgents(ck);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e }, 'listAgents failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });

  // POST /v1/agents
  app.post('/v1/agents', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.createAgent(ck, req.body);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e }, 'createAgent failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });

  // GET /v1/agents/{id}
  app.get<{ Params: { id: string } }>('/v1/agents/:id', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.getAgent(ck, req.params.id);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e, id: req.params.id }, 'getAgent failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });

  // PATCH /v1/agents/{id}
  app.patch<{ Params: { id: string } }>('/v1/agents/:id', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.updateAgent(ck, req.params.id, req.body);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e, id: req.params.id }, 'updateAgent failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });

  // DELETE /v1/agents/{id}
  app.delete<{ Params: { id: string } }>('/v1/agents/:id', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.deleteAgent(ck, req.params.id);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e, id: req.params.id }, 'deleteAgent failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm -C gateway test test/routes/agents.test.ts 2>&1 | tail -20`
Expected: PASS（10 个 it 全过）

- [ ] **Step 5: 跑 typecheck 验证无错**

Run: `pnpm -C gateway typecheck 2>&1 | tail -10`
Expected: 无错误输出（exit 0）

- [ ] **Step 6: 提交**

```bash
cd /home/handdeng/rd-center/my-ai
git add gateway/src/routes/agents.ts gateway/test/routes/agents.test.ts
git commit -m "v6.2: extend /v1/agents with POST/GET-id/PATCH/DELETE + error pass-through"
```

---

## Task 3: sessions.ts 新建 3 端点 + 路由单测

**Files:**

- Create: `gateway/src/routes/sessions.ts`
- Test: `gateway/test/routes/sessions.test.ts`

- [ ] **Step 1: 写失败测试**

新建 `gateway/test/routes/sessions.test.ts`：

```ts
// /v1/sessions 路由层单测：3 端点（POST / GET-id / DELETE）+ 错误码透传矩阵。
import { describe, it, expect } from 'vitest';
import Fastify, { type FastifyInstance } from 'fastify';
import { openDatabase } from '@/db.js';
import { AuthStore } from '@/auth/store.js';
import { authMiddleware } from '@/auth/middleware.js';
import { sessionRoutes } from '@/routes/sessions.js';
import type { CoreClient } from '@/clients/core.js';

type CoreResponse = { status: number; data: unknown };

function buildMockCore(responses: Record<string, CoreResponse> = {}): CoreClient {
  return {
    listAgents: vi(async () => ({ status: 200, data: [] })),
    createAgent: vi(async () => ({ status: 200, data: null })),
    getAgent: vi(async () => ({ status: 200, data: null })),
    updateAgent: vi(async () => ({ status: 200, data: null })),
    deleteAgent: vi(async () => ({ status: 200, data: null })),
    createSession: vi(
      async (_ck: string, _b: unknown) =>
        responses.createSession ?? { status: 200, data: { id: 's1', agentId: 'a1' } },
    ),
    getSession: vi(
      async (_ck: string, _id: string) =>
        responses.getSession ?? { status: 200, data: { id: 's1' } },
    ),
    deleteSession: vi(
      async (_ck: string, _id: string) => responses.deleteSession ?? { status: 200, data: null },
    ),
    listMessages: vi(async () => ({ status: 200, data: [] })),
    postMessage: vi(async () => ({ status: 200, data: null })),
    health: vi(async () => ({ ok: true, service: 'core' })),
  } as unknown as CoreClient;
}

async function buildApp(core: CoreClient): Promise<{ app: FastifyInstance; ckHeader: string }> {
  const store = new AuthStore(openDatabase(':memory:'));
  const ck = 'test-sha256-hash';
  store.insertClient({
    id: ck,
    keyHash: ck,
    name: 'tester',
    createdAt: Date.now(),
    lastSeenAt: Date.now(),
  });

  const app = Fastify({ logger: false });
  app.decorate('core', core);
  app.decorate('authStore', store);
  await app.register(authMiddleware);
  await app.register(async instance => {
    await sessionRoutes(instance, core);
  });
  return { app, ckHeader: ck };
}

describe('/v1/sessions routes', () => {
  it('POST /v1/sessions：200 + 透传 core.data', async () => {
    const core = buildMockCore({
      createSession: { status: 200, data: { id: 's1', agentId: 'a1' } },
    });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'POST',
      url: '/v1/sessions',
      headers: { 'x-client-key': ckHeader, 'content-type': 'application/json' },
      payload: { id: 's1', agentId: 'a1' },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: { id: 's1', agentId: 'a1' }, code: 0, message: 'ok' });
  });

  it('GET /v1/sessions/{id}：200 + 透传 core.data', async () => {
    const core = buildMockCore({ getSession: { status: 200, data: { id: 's1', agentId: 'a1' } } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/sessions/s1',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: { id: 's1', agentId: 'a1' }, code: 0, message: 'ok' });
  });

  it('DELETE /v1/sessions/{id}：200 + 透传 core.data', async () => {
    const core = buildMockCore({ deleteSession: { status: 200, data: null } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'DELETE',
      url: '/v1/sessions/s1',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: null, code: 0, message: 'ok' });
  });

  it('错误码透传：core 404 session_not_found 整包透传', async () => {
    const core = buildMockCore({ getSession: { status: 404, data: null } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/sessions/missing',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(404);
    expect(res.json().data).toBeNull();
  });

  it('gateway 网络层异常 → 502 upstream_error', async () => {
    const core = {
      createSession: vi(async () => {
        throw new Error('ECONNREFUSED');
      }),
    } as unknown as CoreClient;
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'POST',
      url: '/v1/sessions',
      headers: { 'x-client-key': ckHeader, 'content-type': 'application/json' },
      payload: { id: 's1', agentId: 'a1' },
    });
    expect(res.statusCode).toBe(502);
    expect(res.json()).toEqual({ data: null, code: 502, message: 'upstream_error' });
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm -C gateway test test/routes/sessions.test.ts 2>&1 | tail -20`
Expected: FAIL —— "Cannot find module '@/routes/sessions.js'"

- [ ] **Step 3: 写实现**

新建 `gateway/src/routes/sessions.ts`：

```ts
// gateway 透传 /v1/sessions 到 core：3 端点（POST / GET-id / DELETE）。
// 错误码透传：core 4xx/5xx 整包透传（status + data 字段）；gateway 网络层异常 → 502 upstream_error。
import type { FastifyInstance, FastifyRequest } from 'fastify';
import { ok, err } from '../response.js';
import type { CoreClient } from '../clients/core.js';

function getClientKey(req: FastifyRequest): string {
  if (!req.clientCtx) throw new Error('clientCtx not set (auth middleware not run?)');
  return req.clientCtx.id;
}

export async function sessionRoutes(app: FastifyInstance, core: CoreClient) {
  // POST /v1/sessions
  app.post('/v1/sessions', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.createSession(ck, req.body);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e }, 'createSession failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });

  // GET /v1/sessions/{id}
  app.get<{ Params: { id: string } }>('/v1/sessions/:id', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.getSession(ck, req.params.id);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e, id: req.params.id }, 'getSession failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });

  // DELETE /v1/sessions/{id}
  app.delete<{ Params: { id: string } }>('/v1/sessions/:id', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.deleteSession(ck, req.params.id);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e, id: req.params.id }, 'deleteSession failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm -C gateway test test/routes/sessions.test.ts 2>&1 | tail -20`
Expected: PASS（5 个 it 全过）

- [ ] **Step 5: 跑 typecheck 验证无错**

Run: `pnpm -C gateway typecheck 2>&1 | tail -10`
Expected: 无错误输出

- [ ] **Step 6: 提交**

```bash
cd /home/handdeng/rd-center/my-ai
git add gateway/src/routes/sessions.ts gateway/test/routes/sessions.test.ts
git commit -m "v6.2: add /v1/sessions passthrough (POST/GET-id/DELETE)"
```

---

## Task 4: messages.ts 新建 2 端点 + 路由单测

**Files:**

- Create: `gateway/src/routes/messages.ts`
- Test: `gateway/test/routes/messages.test.ts`

- [ ] **Step 1: 写失败测试**

新建 `gateway/test/routes/messages.test.ts`：

```ts
// /v1/sessions/{id}/messages 路由层单测：2 端点（GET / POST）+ 错误码透传矩阵。
import { describe, it, expect } from 'vitest';
import Fastify, { type FastifyInstance } from 'fastify';
import { openDatabase } from '@/db.js';
import { AuthStore } from '@/auth/store.js';
import { authMiddleware } from '@/auth/middleware.js';
import { messagesRoutes } from '@/routes/messages.js';
import type { CoreClient } from '@/clients/core.js';

type CoreResponse = { status: number; data: unknown };

function buildMockCore(responses: Record<string, CoreResponse> = {}): CoreClient {
  return {
    listAgents: vi(async () => ({ status: 200, data: [] })),
    createAgent: vi(async () => ({ status: 200, data: null })),
    getAgent: vi(async () => ({ status: 200, data: null })),
    updateAgent: vi(async () => ({ status: 200, data: null })),
    deleteAgent: vi(async () => ({ status: 200, data: null })),
    createSession: vi(async () => ({ status: 200, data: null })),
    getSession: vi(async () => ({ status: 200, data: null })),
    deleteSession: vi(async () => ({ status: 200, data: null })),
    listMessages: vi(
      async (_ck: string, _sid: string) => responses.listMessages ?? { status: 200, data: [] },
    ),
    postMessage: vi(
      async (_ck: string, _sid: string, _b: unknown) =>
        responses.postMessage ?? {
          status: 200,
          data: { userMessage: { id: 'um' }, assistantMessage: { id: 'am' } },
        },
    ),
    health: vi(async () => ({ ok: true, service: 'core' })),
  } as unknown as CoreClient;
}

async function buildApp(core: CoreClient): Promise<{ app: FastifyInstance; ckHeader: string }> {
  const store = new AuthStore(openDatabase(':memory:'));
  const ck = 'test-sha256-hash';
  store.insertClient({
    id: ck,
    keyHash: ck,
    name: 'tester',
    createdAt: Date.now(),
    lastSeenAt: Date.now(),
  });

  const app = Fastify({ logger: false });
  app.decorate('core', core);
  app.decorate('authStore', store);
  await app.register(authMiddleware);
  await app.register(async instance => {
    await messagesRoutes(instance, core);
  });
  return { app, ckHeader: ck };
}

describe('/v1/sessions/{id}/messages routes', () => {
  it('GET /v1/sessions/{id}/messages：200 + 透传 core.data（messages 列表）', async () => {
    const core = buildMockCore({
      listMessages: { status: 200, data: [{ id: 'm1', role: 'user' }] },
    });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/sessions/s1/messages',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({ data: [{ id: 'm1', role: 'user' }], code: 0, message: 'ok' });
  });

  it('POST /v1/sessions/{id}/messages：200 + 透传 core.data（userMessage + assistantMessage）', async () => {
    const core = buildMockCore({
      postMessage: {
        status: 200,
        data: { userMessage: { id: 'um' }, assistantMessage: { id: 'am', content: 'hi' } },
      },
    });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'POST',
      url: '/v1/sessions/s1/messages',
      headers: { 'x-client-key': ckHeader, 'content-type': 'application/json' },
      payload: { id: 'um', content: 'hello' },
    });
    expect(res.statusCode).toBe(200);
    expect(res.json()).toEqual({
      data: { userMessage: { id: 'um' }, assistantMessage: { id: 'am', content: 'hi' } },
      code: 0,
      message: 'ok',
    });
  });

  it('错误码透传：core 404 session_not_found 整包透传', async () => {
    const core = buildMockCore({ listMessages: { status: 404, data: null } });
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'GET',
      url: '/v1/sessions/missing/messages',
      headers: { 'x-client-key': ckHeader },
    });
    expect(res.statusCode).toBe(404);
    expect(res.json().data).toBeNull();
  });

  it('gateway 网络层异常 → 502 upstream_error', async () => {
    const core = {
      postMessage: vi(async () => {
        throw new Error('ECONNREFUSED');
      }),
    } as unknown as CoreClient;
    const { app, ckHeader } = await buildApp(core);
    const res = await app.inject({
      method: 'POST',
      url: '/v1/sessions/s1/messages',
      headers: { 'x-client-key': ckHeader, 'content-type': 'application/json' },
      payload: { id: 'um', content: 'hello' },
    });
    expect(res.statusCode).toBe(502);
    expect(res.json()).toEqual({ data: null, code: 502, message: 'upstream_error' });
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm -C gateway test test/routes/messages.test.ts 2>&1 | tail -20`
Expected: FAIL —— "Cannot find module '@/routes/messages.js'"

- [ ] **Step 3: 写实现**

新建 `gateway/src/routes/messages.ts`：

```ts
// gateway 透传 /v1/sessions/{id}/messages 到 core：2 端点（GET / POST）。
// 错误码透传：core 4xx/5xx 整包透传（status + data 字段）；gateway 网络层异常 → 502 upstream_error。
import type { FastifyInstance, FastifyRequest } from 'fastify';
import { ok, err } from '../response.js';
import type { CoreClient } from '../clients/core.js';

function getClientKey(req: FastifyRequest): string {
  if (!req.clientCtx) throw new Error('clientCtx not set (auth middleware not run?)');
  return req.clientCtx.id;
}

export async function messagesRoutes(app: FastifyInstance, core: CoreClient) {
  // GET /v1/sessions/{id}/messages
  app.get<{ Params: { id: string } }>('/v1/sessions/:id/messages', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.listMessages(ck, req.params.id);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e, sessionId: req.params.id }, 'listMessages failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });

  // POST /v1/sessions/{id}/messages（同步 chat，取代 v1 /v1/chat）
  app.post<{ Params: { id: string } }>('/v1/sessions/:id/messages', async (req, reply) => {
    const ck = getClientKey(req);
    try {
      const { status, data } = await core.postMessage(ck, req.params.id, req.body);
      return reply.code(status).send(ok(data));
    } catch (e) {
      req.log.error({ err: e, sessionId: req.params.id }, 'postMessage failed');
      return reply.code(502).send(err(502, 'upstream_error'));
    }
  });
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm -C gateway test test/routes/messages.test.ts 2>&1 | tail -20`
Expected: PASS（4 个 it 全过）

- [ ] **Step 5: 跑 typecheck 验证无错**

Run: `pnpm -C gateway typecheck 2>&1 | tail -10`
Expected: 无错误输出

- [ ] **Step 6: 提交**

```bash
cd /home/handdeng/rd-center/my-ai
git add gateway/src/routes/messages.ts gateway/test/routes/messages.test.ts
git commit -m "v6.2: add /v1/sessions/{id}/messages passthrough (GET/POST)"
```

---

## Task 5: server.ts 注册 sessions / messages 路由

**Files:**

- Modify: `gateway/src/server.ts`

- [ ] **Step 1: 修改 server.ts**

在 `gateway/src/server.ts` 中：

1. 在 import 块增：

```ts
import { sessionRoutes } from './routes/sessions.js';
import { messagesRoutes } from './routes/messages.js';
```

2. 找到 `await app.register(async instance => { await agentRoutes(instance, core); });` 块（business routes 段），改为：

```ts
await app.register(async instance => {
  await agentRoutes(instance, core);
  await sessionRoutes(instance, core);
  await messagesRoutes(instance, core);
});
```

完整业务 routes 段（参考上下文）：

```ts
// 业务 routes（需鉴权）
await app.register(async instance => {
  await agentRoutes(instance, core);
  await sessionRoutes(instance, core);
  await messagesRoutes(instance, core);
});
```

- [ ] **Step 2: 跑 typecheck 验证无错**

Run: `pnpm -C gateway typecheck 2>&1 | tail -10`
Expected: 无错误输出

- [ ] **Step 3: 跑全测试套件验证无回归**

Run: `pnpm -C gateway test 2>&1 | tail -30`
Expected: 全绿 —— 之前所有测试 + 新增 19 个 it（agents 10 + sessions 5 + messages 4）

- [ ] **Step 4: 提交**

```bash
cd /home/handdeng/rd-center/my-ai
git add gateway/src/server.ts
git commit -m "v6.2: register sessionRoutes + messagesRoutes in buildServer"
```

---

## Task 6: Integration 测试（mock core HTTP server + 真实 buildServer + 跨 clientKey 共享）

**Files:**

- Create: `gateway/test/integration/gateway-core.test.ts`

- [ ] **Step 1: 写失败测试 —— 端到端 + 头注入断言 + 跨 clientKey + 网络异常**

新建 `gateway/test/integration/gateway-core.test.ts`：

```ts
// Gateway 端到端 integration：起一个 mock core HTTP server + 真实 buildServer + 真实 AuthStore。
// 验证：
//   1. 头注入：gateway→core 真的挂上 X-Internal-Client-Key（值 = req.clientCtx.id = sha256 hash）
//   2. 跨 clientKey 共享 session：v6.1 决策 16 —— clientKeyA 创建，clientKeyB GET 同一 session 也能读
//   3. 网络层异常：mock core 关闭连接 → gateway 502 upstream_error
//   4. 错误码端到端：mock core 返回 404 / 409 / 502 → gateway 整包透传
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { createServer, type Server, type IncomingMessage, type ServerResponse } from 'node:http';
import { openDatabase } from '@/db.js';
import { AuthStore } from '@/auth/store.js';
import { buildServer } from '@/server.js';
import { loadConfig } from '@/config.js';
import type { Compat } from '@/compat/load.js';
import type { FastifyInstance } from 'fastify';

let mockCore: Server;
let mockCoreUrl: string;
let capturedRequests: Array<{
  method?: string;
  url?: string;
  headers: Record<string, string | string[] | undefined>;
  body?: string;
}> = [];

const fakeCompat: Compat = { version: '9.9.9', upstream: { core: '>=0.0.2 <0.1.0' } };

beforeAll(async () => {
  // mock core 端：用 map 存 sessions，支持跨 clientKey 共享
  const sessions = new Map<
    string,
    { id: string; agentId: string; clientKey: string; createdAt: string; updatedAt: string }
  >();

  mockCore = createServer((req: IncomingMessage, res: ServerResponse) => {
    const chunks: Buffer[] = [];
    req.on('data', c => chunks.push(c));
    req.on('end', () => {
      capturedRequests.push({
        method: req.method,
        url: req.url,
        headers: req.headers,
        body: chunks.length > 0 ? Buffer.concat(chunks).toString('utf8') : undefined,
      });

      const ck = req.headers['x-internal-client-key'];
      if (typeof ck !== 'string') {
        res.statusCode = 401;
        res.setHeader('Content-Type', 'application/json');
        res.end(JSON.stringify({ data: null, code: 401, message: 'unauthorized' }));
        return;
      }

      // POST /v1/sessions
      if (req.url === '/v1/sessions' && req.method === 'POST') {
        const body = JSON.parse(chunks.toString('utf8')) as { id: string; agentId: string };
        const session = {
          id: body.id,
          agentId: body.agentId,
          clientKey: ck,
          createdAt: new Date().toISOString(),
          updatedAt: new Date().toISOString(),
        };
        sessions.set(session.id, session);
        res.statusCode = 200;
        res.setHeader('Content-Type', 'application/json');
        res.end(JSON.stringify({ data: session, code: 0, message: 'ok' }));
        return;
      }

      // GET /v1/sessions/{id}（跨 clientKey 共享：不做 clientKey 校验）
      const getMatch = req.url?.match(/^\/v1\/sessions\/([^/]+)$/);
      if (getMatch && req.method === 'GET') {
        const sid = getMatch[1];
        const session = sessions.get(sid);
        if (!session) {
          res.statusCode = 404;
          res.setHeader('Content-Type', 'application/json');
          res.end(JSON.stringify({ data: null, code: 404, message: 'session_not_found' }));
          return;
        }
        res.statusCode = 200;
        res.setHeader('Content-Type', 'application/json');
        res.end(JSON.stringify({ data: session, code: 0, message: 'ok' }));
        return;
      }

      // GET /v1/agents（list）
      if (req.url === '/v1/agents' && req.method === 'GET') {
        res.statusCode = 200;
        res.setHeader('Content-Type', 'application/json');
        res.end(JSON.stringify({ data: [], code: 0, message: 'ok' }));
        return;
      }

      // POST /v1/agents（name 冲突测试用）
      if (req.url === '/v1/agents' && req.method === 'POST') {
        const body = JSON.parse(chunks.toString('utf8')) as { name?: string };
        if (body.name === 'duplicate') {
          res.statusCode = 409;
          res.setHeader('Content-Type', 'application/json');
          res.end(JSON.stringify({ data: null, code: 409, message: 'agent_name_conflict' }));
          return;
        }
        res.statusCode = 200;
        res.setHeader('Content-Type', 'application/json');
        res.end(JSON.stringify({ data: { id: 'new', name: body.name }, code: 0, message: 'ok' }));
        return;
      }

      // 默认
      res.statusCode = 404;
      res.setHeader('Content-Type', 'application/json');
      res.end(JSON.stringify({ data: null, code: 404, message: 'not_found' }));
    });
  });

  await new Promise<void>(resolve => mockCore.listen(0, '127.0.0.1', resolve));
  const addr = mockCore.address();
  if (typeof addr === 'string' || addr === null) throw new Error('unexpected address');
  mockCoreUrl = `http://127.0.0.1:${addr.port}`;
});

afterAll(async () => {
  await new Promise<void>((resolve, reject) =>
    mockCore.close(err => (err ? reject(err) : resolve())),
  );
});

async function buildApp(): Promise<{ app: FastifyInstance; ckA: string; ckB: string }> {
  const store = new AuthStore(openDatabase(':memory:'));
  const ckA = 'sha256-hash-A';
  const ckB = 'sha256-hash-B';
  store.insertClient({
    id: ckA,
    keyHash: ckA,
    name: 'clientA',
    createdAt: Date.now(),
    lastSeenAt: Date.now(),
  });
  store.insertClient({
    id: ckB,
    keyHash: ckB,
    name: 'clientB',
    createdAt: Date.now(),
    lastSeenAt: Date.now(),
  });

  const cfg = loadConfig({
    PORT: '0',
    HOST: '127.0.0.1',
    CORE_URL: mockCoreUrl,
    LOG_LEVEL: 'fatal',
    CORS_ORIGINS: '*',
    GATEWAY_PAIRING_PUBLIC: 'false',
    GATEWAY_DB_PATH: ':memory:',
  });

  const app = await buildServer(cfg, fakeCompat, store);
  return { app, ckA, ckB };
}

describe('Integration: gateway → core', () => {
  it('头注入：X-Internal-Client-Key = req.clientCtx.id (sha256 hash) 出现在 mock core 请求上', async () => {
    capturedRequests = [];
    const { app, ckA } = await buildApp();
    await app.inject({ method: 'GET', url: '/v1/agents', headers: { 'x-client-key': ckA } });
    // 找 GET /v1/agents 请求
    const listReq = capturedRequests.find(r => r.method === 'GET' && r.url === '/v1/agents');
    expect(listReq).toBeDefined();
    expect(listReq!.headers['x-internal-client-key']).toBe(ckA);
  });

  it('跨 clientKey 共享 session：clientKeyA 创建 → clientKeyB GET 也能读到（v6.1 决策 16）', async () => {
    capturedRequests = [];
    const { app, ckA, ckB } = await buildApp();
    // clientA 创建 session
    const createRes = await app.inject({
      method: 'POST',
      url: '/v1/sessions',
      headers: { 'x-client-key': ckA, 'content-type': 'application/json' },
      payload: { id: 's-shared', agentId: 'a1' },
    });
    expect(createRes.statusCode).toBe(200);
    expect(createRes.json().data.clientKey).toBe(ckA); // 写的是 A 的 hash

    // clientB GET 同一 session
    const getRes = await app.inject({
      method: 'GET',
      url: '/v1/sessions/s-shared',
      headers: { 'x-client-key': ckB },
    });
    expect(getRes.statusCode).toBe(200);
    expect(getRes.json().data.id).toBe('s-shared');
  });

  it('错误码端到端：core 返回 409 agent_name_conflict → gateway 整包透传', async () => {
    const { app, ckA } = await buildApp();
    const res = await app.inject({
      method: 'POST',
      url: '/v1/agents',
      headers: { 'x-client-key': ckA, 'content-type': 'application/json' },
      payload: { id: 'x', name: 'duplicate' },
    });
    expect(res.statusCode).toBe(409);
    expect(res.json()).toEqual({ data: null, code: 409, message: 'agent_name_conflict' });
  });

  it('错误码端到端：core 返回 404 session_not_found → gateway 整包透传', async () => {
    const { app, ckA } = await buildApp();
    const res = await app.inject({
      method: 'GET',
      url: '/v1/sessions/nonexistent',
      headers: { 'x-client-key': ckA },
    });
    expect(res.statusCode).toBe(404);
    expect(res.json().data).toBeNull();
  });

  it('网络层异常：mock core 关闭连接 → gateway 502 upstream_error', async () => {
    // 关掉 mock core，再发请求
    const { app, ckA } = await buildApp();
    await new Promise<void>((resolve, reject) =>
      mockCore.close(err => (err ? reject(err) : resolve())),
    );
    // 起一个新的空 server（拒绝连接）：监听 0 拿端口但立即 close
    const { createServer } = await import('node:http');
    const dummy = createServer(() => {});
    await new Promise<void>(resolve => dummy.listen(0, '127.0.0.1', resolve));
    const addr = dummy.address();
    if (typeof addr === 'string' || addr === null) throw new Error('unexpected address');
    const deadUrl = `http://127.0.0.1:${addr.port}`;
    await new Promise<void>((resolve, reject) =>
      dummy.close(err => (err ? reject(err) : resolve())),
    );

    // 重新 build app，指向 deadUrl
    const store = new AuthStore(openDatabase(':memory:'));
    const cfg = loadConfig({
      PORT: '0',
      HOST: '127.0.0.1',
      CORE_URL: deadUrl,
      LOG_LEVEL: 'fatal',
      CORS_ORIGINS: '*',
      GATEWAY_PAIRING_PUBLIC: 'false',
      GATEWAY_DB_PATH: ':memory:',
    });
    const deadApp = await buildServer(cfg, fakeCompat, store);
    store.insertClient({
      id: ckA,
      keyHash: ckA,
      name: 'clientA',
      createdAt: Date.now(),
      lastSeenAt: Date.now(),
    });

    const res = await deadApp.inject({
      method: 'GET',
      url: '/v1/agents',
      headers: { 'x-client-key': ckA },
    });
    expect(res.statusCode).toBe(502);
    expect(res.json()).toEqual({ data: null, code: 502, message: 'upstream_error' });
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `pnpm -C gateway test test/integration/gateway-core.test.ts 2>&1 | tail -30`
Expected: FAIL —— 多个 it 失败（因为这是新建测试）

- [ ] **Step 3: 实现无需新增代码（pure 集成测）**

无新增源文件；如测试失败，先排查：

- 检查 mock core 端路由路径匹配
- 检查 loadConfig 是否支持 GATEWAY_DB_PATH=':memory:'
- 检查 buildServer 是否接受 openDatabase(':memory:') 给的 store

如以上都 OK，跑到全绿即可。

- [ ] **Step 4: 跑测试验证通过**

Run: `pnpm -C gateway test test/integration/gateway-core.test.ts 2>&1 | tail -30`
Expected: PASS（5 个 it 全过）

- [ ] **Step 5: 跑 typecheck 验证无错**

Run: `pnpm -C gateway typecheck 2>&1 | tail -10`
Expected: 无错误输出

- [ ] **Step 6: 提交**

```bash
cd /home/handdeng/rd-center/my-ai
git add gateway/test/integration/gateway-core.test.ts
git commit -m "v6.2: add gateway-core integration test (header injection, cross-clientKey, network err)"
```

---

## Task 7: 收尾（全 typecheck / 全测试 / lint / 文档）

**Files:**

- Read: `versions/v6.2.md` 复核
- Verify: 三端 typecheck + 全测试 + lint

- [ ] **Step 1: 跑三端 typecheck**

Run:

```bash
cd /home/handdeng/rd-center/my-ai
pnpm -C gateway typecheck 2>&1 | tail -10
pnpm -C core typecheck 2>&1 | tail -10
pnpm -C client typecheck 2>&1 | tail -10
```

Expected: 三端都无错误（exit 0）

- [ ] **Step 2: 跑三端全测试套件**

Run:

```bash
cd /home/handdeng/rd-center/my-ai
pnpm -C gateway test 2>&1 | tail -20
pnpm -C core test 2>&1 | tail -20
pnpm -C client test 2>&1 | tail -20
```

Expected:

- gateway：原 20+ 测试 + 新增 19 路由单测（agents 10 + sessions 5 + messages 4）+ 5 integration = 40+ 全绿
- core / client：现状全绿（v6.2 不动 core/client）

- [ ] **Step 3: 跑 gateway 端 lint**

Run: `pnpm -C gateway lint 2>&1 | tail -20`
Expected: 无 error / 无 warning（`--max-warnings 0`）

- [ ] **Step 4: 复核 v6.2.md 与实际实现对齐**

读 `versions/v6.2.md`：

- §5.4.2 `core.ts` 方法签名 ↔ 实际 `core.ts` 内容
- §5.4.3 路由 handler 模板 ↔ 实际 `agents.ts` / `sessions.ts` / `messages.ts`
- §5.5.2 错误码矩阵 ↔ 实际测试覆盖
- §6 变更清单 ↔ 实际 `git diff dev-20260610..HEAD --stat`

如有不一致：修补文档 + amend 提交（如有 commit 注释差异）或单独提交（如有实际行为差异）。

- [ ] **Step 5: 写一个汇总 commit 描述**

```bash
cd /home/handdeng/rd-center/my-ai
git log --oneline dev-20260610..HEAD
```

Expected: 应有 6 个 commit：

1. `v6.2: extend CoreClient with 9 business methods + private call() helper`
2. `v6.2: extend /v1/agents with POST/GET-id/PATCH/DELETE + error pass-through`
3. `v6.2: add /v1/sessions passthrough (POST/GET-id/DELETE)`
4. `v6.2: add /v1/sessions/{id}/messages passthrough (GET/POST)`
5. `v6.2: register sessionRoutes + messagesRoutes in buildServer`
6. `v6.2: add gateway-core integration test (header injection, cross-clientKey, network err)`

- [ ] **Step 6: 不做 push / 不做 PR merge**

按 CLAUDE.md + auto-memory 约定：

- push：可直接推（本地能测的都测过即可，含 CI-only 改动本地可做的部分）
- PR merge/squash：必须用户明确批准

到这一步**停下**，等用户决定 push / 提 PR。

---

## Self-Review（plan 写完后）

### 1. Spec coverage（v6.2.md 五章 → plan 任务）

| spec 章节                                  | 覆盖任务                                                                                      |
| ------------------------------------------ | --------------------------------------------------------------------------------------------- |
| 5.1 架构总览                               | Task 0（preflight 读基线） + Task 5（server 注册）                                            |
| 5.2 API 端点表（10 端点）                  | Task 2（agents 5）+ Task 3（sessions 3）+ Task 4（messages 2）                                |
| 5.3 核心数据流：发一条消息                 | Task 6（integration 覆盖发消息路径 + 头注入断言）                                             |
| 5.4 CoreClient 扩展（11 方法 + call 辅助） | Task 1（11 方法 + call）                                                                      |
| 5.5 错误码透传矩阵                         | Task 2-4（每个路由测含 4xx/5xx 透传 case） + Task 6（integration 端到端覆盖 409 / 404 / 502） |
| 5.6 测试策略（双层）                       | Task 1-4（路由单测） + Task 6（integration）                                                  |

✅ 所有 spec 章节有对应任务。

### 2. Placeholder scan

`grep -nE 'TODO|TBD|FIXME|XXX|\?\?\?'` 在 plan 上 = 0 命中。

✅ 无 placeholder。

### 3. Type consistency

- `CoreClient.listAgents(clientKey: string)` 在 Task 1 定义 ↔ Task 2 测试 buildMockCore 用 `listAgents: vi(async (_ck) => ...)` 签名 ↔ Task 2 路由 handler 调 `core.listAgents(ck)` 一致 ✅
- `CoreClient.call(method, path, clientKey, body?)` 在 Task 1 私有；9 新方法都走 `this.call('METHOD', path, ck, body?)` 一致 ✅
- `getClientKey(req): string` 在 3 个路由文件重复定义（agents / sessions / messages）—— v6.2 spec §5.4.3 路由 handler 模板明确"5-6 行 / 端点"+ 复用逻辑；考虑提到共享 helper，但 spec 决策 11 + plan 决策"v3 现状无 helper"暂不抽。**Note to self**：v6.3+ 评估抽 `@/lib/get-client-key.ts` 共享。
- `req.clientCtx` 类型断言：v3 middleware 类型 `clientCtx?: { id: string; name: string | null }`；`getClientKey` 运行时断言 `!req.clientCtx` 抛错。✅

### 4. 已知小问题（不影响计划执行）

- **getClientKey 重复 3 次**：可考虑抽 helper；当前决定"不抽"（YAGNI；3 文件各自 5-10 行 handler）；v6.3+ 评估
- **Task 6 integration 网络异常测试**：因 mock core 关闭 + 重新 buildApp 较繁琐，测试代码稍长；可读性 OK 但单元可拆。已记
- **CLAUDE.md 注释中文规范**：每个 .ts 都有 `// ...` 注释；所有 task 代码注释保持中文 ✅
- **commit author**：仅 `git commit -m`；不加 Co-Authored-By Claude trailer ✅
- **dev 分支**：Task 0 决定"在 dev-20260610 续写 or 新建 dev-20260611"——按用户实际选择

---

## 完成标准

- 6 个 commit（每个 task 一个 + Task 0 不 commit 因只是读 / 跑基线）
- gateway 端 typecheck + test + lint 全绿
- core / client 端测试不受影响
- 文档 `versions/v6.2.md` 与实际代码对齐
- 停下等用户 push / 提 PR
