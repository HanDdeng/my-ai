# v5 — 客户端 i18n 架构 + zh-CN/en 双语种 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 client 端引入 i18next + react-i18next 架构；抽取 6 个组件（`App` + 5 个 `src/components/`）的中英混排文案到资源文件；交付简体中文（zh-CN）与英文（en）两套语种；Header 顶栏加 `<LanguageSwitcher />` 切换按钮，localStorage 持久化偏好；新增 `i18n-check` CI job 校验翻译完整性；client 0.0.3 → 0.0.4（gateway/core 不动，compat range 不变）。

**Architecture:** i18next 静态 import 资源进 bundle（Tauri file:// 协议无 CORS 顾虑）；点分嵌套 key 路径（feature / domain → 子模块 → 状态 / 类型）；`useTranslation()` hooks API 拿 `t` 函数；`fallbackLng='zh-CN'` 缺 key 自动降级；测试 setup 钉死 `i18n.changeLanguage('zh-CN')` 避免依赖 `navigator.language`；测试断言改 `screen.getByText(i18n.t(key))` 让测试与译文解耦。

**Tech Stack:** i18next 23.x、react-i18next 15.x、React 18、Tauri 2、pnpm 10 workspaces、Vitest 2.1、TypeScript 5.5、commitlint、husky

**Spec:** [`versions/v5.md`](../../../versions/v5.md)（commit `2254992` on `dev-20260609`）

---

## 总览：1 个 Phase，16 个 Task

| Task | 内容                                                                             | 涉及文件 / 备注                                  |
| ---- | -------------------------------------------------------------------------------- | ------------------------------------------------ |
| 1    | 准备工作 + 前置状态确认                                                          | 0                                                |
| 2    | 加 i18next / react-i18next 依赖到 pnpm catalog + client                          | 2 (`pnpm-workspace.yaml`, `client/package.json`) |
| 3    | 新增 `client/src/i18n/locales/{en,zh-CN}/translation.json`                       | 2 JSON                                           |
| 4    | 新增 `client/src/i18n/index.ts`（i18next init + 检测链）                         | 1                                                |
| 5    | `client/test/setup.ts` 钉死 zh-CN                                                | 1                                                |
| 6    | 重构 `MismatchBanner` 用 `t()`                                                   | 1 组件 + 1 测试                                  |
| 7    | 重构 `PairBanner` 用 `t()`                                                       | 1 组件 + 1 测试                                  |
| 8    | 重构 `Settings` 用 `t()`（`STATUS_LABEL` 改查 i18n）                             | 1 组件 + 1 测试                                  |
| 9    | 重构 `PairDialog` 用 `t()`（含 `friendlyApiError` 错误码 map）                   | 1 组件 + 1 测试                                  |
| 10   | 重构 `ThemeToggle` 用 `t()`                                                      | 1 组件 + 1 测试                                  |
| 11   | 新增 `client/src/i18n/switch.tsx`（`<LanguageSwitcher />`）                      | 1 组件 + 1 测试                                  |
| 12   | 重构 `App.tsx`（header / footer t() + 加 LanguageSwitcher）                      | 1                                                |
| 13   | 检查 `App.test.tsx`（review 决定是否改）                                         | 0-1                                              |
| 14   | 新增 `scripts/check-i18n.mjs` + 测试                                             | 1 脚本 + 1 测试                                  |
| 15   | 新增 `.github/workflows/i18n-check.yml`                                          | 1                                                |
| 16   | 版本 bump（client/package.json + tauri.conf.json + compat-matrix）+ 全工作区验证 | 3 + 1 commit                                     |

**约束**：

- 每 task 完成后跑 `pnpm -r typecheck && pnpm -r test` 两件套，全绿才能进下个 task
- 改写 import / 资源 / 组件 后必须看到 `pnpm -r test` 全绿，**才**能 commit
- 失败回退到上一个 commit（用 `git reset --hard HEAD` 而非 `git revert`，因为还没 push）
- 所有 commit author 用 `handdeng`（CLAUDE.md 规则）
- 所有 commit subject 用 conventional commit 格式（commitlint 强制）
- 任何依赖 / 版本 / 配置文件变更后跑一次 `pnpm -r lint` 兜底

**关键变更提示**：

- 所有 import 一律 `'@/...'` 形式（v4 已配 `paths: { "@/*": ["./src/*"] }`）
- i18n 资源文件用纯 JSON，**不要**在 JSON 里写 TypeScript 注释
- `t('key')` 缺 key 时 i18next 会返回 key 字面量（`returnNull: false` 时）——但因为 `fallbackLng='zh-CN'`，会回退到 zh-CN 的值，不会显示 key
- 错误码 400-502 的本地化文案进 `errors.api.<code>`，**不要**在 `friendlyApiError` 函数体内留 `Record<number, string>`
- 测试 setup 钉死 `zh-CN` 在 `client/test/setup.ts`（vitest setupFiles）
- 切语种时仅 `i18n.changeLanguage(next)` + `localStorage.setItem('my-ai:lang', next)`；**不要**派发事件、不要刷新页面
- a11y 属性值（`role="alert"` / `aria-hidden="true"` / `data-state` / `data-ok` / `data-current`）**不进** i18n 资源

---

## Phase 1：客户端 i18n 实施

### Task 1：准备工作 + 前置状态确认

**Files:** 无

- [ ] **Step 1：确认在 dev-20260609 分支**

Run:

```bash
git branch --show-current
```

Expected: `dev-20260609`

如果不是，停下来；不要在其他分支继续。先 `git checkout dev-20260609`。

- [ ] **Step 2：确认工作区干净**

Run:

```bash
git status
```

Expected: `nothing to commit, working tree clean`

如有未提交改动，先 `git stash` 或 `git commit`，绝不留半成品进 v5 实施。

- [ ] **Step 3：确认 v5.md 已 commit**

Run:

```bash
git log --oneline -3
```

Expected: 顶部包含 `docs(v5): 客户端 i18n 架构 + zh-CN/en 双语种（设计文档）`

- [ ] **Step 4：确认 pnpm install 已跑**

Run:

```bash
pnpm -v && node -v
```

Expected: `pnpm` 10.x，`node` 20.x

- [ ] **Step 5：基线全绿**

Run:

```bash
pnpm -r typecheck && pnpm -r test
```

Expected: 三个子项目 typecheck Done；163 个测试全过

如有失败，停下来；不要在 baseline 不绿时继续。

- [ ] **Step 6：commit 不需要**

无文件改动。

---

### Task 2：加 i18next / react-i18next 依赖

**Files:**

- Modify: `pnpm-workspace.yaml`
- Modify: `client/package.json`

- [ ] **Step 1：在 pnpm-workspace.yaml catalog 块加 i18next + react-i18next**

打开 `pnpm-workspace.yaml`，找到 `catalog:` 块（v3 起保留其他依赖）。在块末尾追加：

```yaml
i18next: ^23.16.4
react-i18next: ^15.1.0
```

注意缩进与现有 entry 一致（catalog 块是 2 空格缩进，value 在 key 后空一格）。

- [ ] **Step 2：在 client/package.json dependencies 加 i18next / react-i18next**

打开 `client/package.json`，在 `"dependencies"` 块内（按字母顺序插在 `react-dom` 之后）：

```json
    "i18next": "catalog:",
    "react-i18next": "catalog:",
```

- [ ] **Step 3：跑 pnpm install**

Run:

```bash
pnpm install
```

Expected: 装好 `i18next` 与 `react-i18next`，无错误。`pnpm-lock.yaml` 改动属正常。

- [ ] **Step 4：跑 typecheck 验证类型可用**

Run:

```bash
pnpm -r typecheck
```

Expected: 三个子项目 typecheck Done（此时还没 import i18next，类型不验证；只确保 install 没坏其他包）

- [ ] **Step 5：commit**

```bash
git add pnpm-workspace.yaml client/package.json pnpm-lock.yaml
git commit -m "build(v5): 引入 i18next + react-i18next 依赖"
```

---

### Task 3：新增 i18n 资源文件

**Files:**

- Create: `client/src/i18n/locales/zh-CN/translation.json`
- Create: `client/src/i18n/locales/en/translation.json`

- [ ] **Step 1：建目录 + 写 zh-CN 资源**

Run:

```bash
mkdir -p client/src/i18n/locales/zh-CN
```

创建 `client/src/i18n/locales/zh-CN/translation.json`，内容（直接复制，覆盖原文件）：

```json
{
  "app": {
    "brand": "my-ai",
    "title": "GATEWAY PAIR",
    "titleEm": "PAIR",
    "subtitle": {
      "line1": "REMOTE PAIRING & AUTH",
      "sep": "·",
      "line2": "CLIENT {{version}}"
    },
    "footer": {
      "copy": "© my-ai · local control",
      "date": "━━ {{date}}"
    }
  },
  "settings": {
    "label": "Gateway 状态",
    "statusHead": "STATUS / GATEWAY",
    "urlAria": "Gateway URL",
    "test": "测试",
    "gatewayLabel": "网关",
    "versionPrefix": "GATEWAY",
    "status": {
      "PAIRING": "正在测试…",
      "HEALTHY": "配对成功",
      "MISMATCH": "版本不匹配",
      "PAIR_FAILED": "连接失败"
    }
  },
  "pair": {
    "banner": {
      "needPair": {
        "kicker": "待配对",
        "message": "尚未配对。请先完成网关配对。"
      },
      "needRepair": {
        "kicker": "⚠ 重新配对",
        "message": "上次的 clientKey 已失效，请发起新的配对请求。"
      },
      "actions": {
        "goPair": "去配对",
        "clear": "清除配对"
      }
    },
    "dialog": {
      "title": "配对网关",
      "sub": "PAIRING / STEP 01",
      "fields": {
        "url": {
          "label": "Gateway URL",
          "req": "REQ",
          "placeholder": "http://127.0.0.1:8787"
        },
        "pairKey": {
          "label": "Pair Key",
          "opt": "OPT",
          "placeholder": "私有模式凭证",
          "aria": "Pair Key (可选)"
        },
        "name": {
          "label": "客户端名",
          "opt": "OPT",
          "placeholder": "alice-laptop",
          "aria": "客户端名"
        }
      },
      "token": {
        "label": "配对确认码",
        "hintHead": "在网关所在主机终端运行 ↓"
      },
      "status": {
        "connecting": "正在连接网关…",
        "pairing": "正在配对…",
        "pending": "等待对方确认…"
      },
      "actions": {
        "cancel": "取消",
        "submit": "提交",
        "copy": "复制",
        "copied": "已复制"
      },
      "errors": {
        "network": "连不上网关",
        "pairFail": "配对失败",
        "missingToken": "配对失败：网关响应缺少确认码",
        "timeout": "对方长时间未确认（5 分钟），请重试或联系管理员"
      }
    }
  },
  "mismatch": {
    "tag": "Mismatch",
    "message": "Gateway {{version}} 超出 client 兼容范围 ({{range}})。部分功能可能不可用，建议升级 gateway。",
    "dismiss": "关闭"
  },
  "errors": {
    "network": {
      "fetchFail": "网络连不上，请检查 URL / 端口 / 防火墙",
      "notMyAi": "目标地址返回的不是 my-ai 网关响应（确认端口/URL 没指错）",
      "unknown": "网络异常（未知错误）"
    },
    "api": {
      "400": "请求格式有误",
      "401": "客户端标识无效或缺失",
      "403": "无权限访问",
      "404": "资源不存在（确认码可能已过期）",
      "500": "网关内部错误",
      "502": "上游服务不可用"
    }
  },
  "theme": {
    "light": "LIGHT",
    "dark": "DARK",
    "ariaCurrent": "当前 {{state}}色主题，点击切换到 {{next}}色"
  },
  "lang": {
    "zh": "中",
    "en": "EN",
    "aria": "切换语言，当前 {{current}}"
  }
}
```

- [ ] **Step 2：建目录 + 写 en 资源**

Run:

```bash
mkdir -p client/src/i18n/locales/en
```

创建 `client/src/i18n/locales/en/translation.json`，内容：

```json
{
  "app": {
    "brand": "my-ai",
    "title": "GATEWAY PAIR",
    "titleEm": "PAIR",
    "subtitle": {
      "line1": "REMOTE PAIRING & AUTH",
      "sep": "·",
      "line2": "CLIENT {{version}}"
    },
    "footer": {
      "copy": "© my-ai · local control",
      "date": "━━ {{date}}"
    }
  },
  "settings": {
    "label": "Gateway Status",
    "statusHead": "STATUS / GATEWAY",
    "urlAria": "Gateway URL",
    "test": "Test",
    "gatewayLabel": "Gateway",
    "versionPrefix": "GATEWAY",
    "status": {
      "PAIRING": "Testing…",
      "HEALTHY": "Paired",
      "MISMATCH": "Version Mismatch",
      "PAIR_FAILED": "Connection Failed"
    }
  },
  "pair": {
    "banner": {
      "needPair": {
        "kicker": "Need Pair",
        "message": "Not paired yet. Please complete gateway pairing."
      },
      "needRepair": {
        "kicker": "⚠ Repair Pairing",
        "message": "Previous clientKey is no longer valid. Please start a new pairing request."
      },
      "actions": {
        "goPair": "Go to Pair",
        "clear": "Clear Pairing"
      }
    },
    "dialog": {
      "title": "Pair Gateway",
      "sub": "PAIRING / STEP 01",
      "fields": {
        "url": {
          "label": "Gateway URL",
          "req": "REQ",
          "placeholder": "http://127.0.0.1:8787"
        },
        "pairKey": {
          "label": "Pair Key",
          "opt": "OPT",
          "placeholder": "Private mode credential",
          "aria": "Pair Key (optional)"
        },
        "name": {
          "label": "Client Name",
          "opt": "OPT",
          "placeholder": "alice-laptop",
          "aria": "Client Name"
        }
      },
      "token": {
        "label": "Pairing Token",
        "hintHead": "Run on the gateway host terminal ↓"
      },
      "status": {
        "connecting": "Connecting to gateway…",
        "pairing": "Pairing…",
        "pending": "Waiting for confirmation…"
      },
      "actions": {
        "cancel": "Cancel",
        "submit": "Submit",
        "copy": "Copy",
        "copied": "Copied"
      },
      "errors": {
        "network": "Cannot reach gateway",
        "pairFail": "Pairing failed",
        "missingToken": "Pairing failed: gateway response is missing the confirmation token",
        "timeout": "Confirmation timed out (5 minutes). Please retry or contact the administrator."
      }
    }
  },
  "mismatch": {
    "tag": "Mismatch",
    "message": "Gateway {{version}} is out of the client compat range ({{range}}). Some features may not work; consider upgrading the gateway.",
    "dismiss": "Dismiss"
  },
  "errors": {
    "network": {
      "fetchFail": "Network unreachable. Check URL / port / firewall.",
      "notMyAi": "The target address is not a my-ai gateway response (check the port/URL).",
      "unknown": "Network error (unknown)"
    },
    "api": {
      "400": "Bad request",
      "401": "Client key invalid or missing",
      "403": "Forbidden",
      "404": "Resource not found (the token may have expired)",
      "500": "Gateway internal error",
      "502": "Upstream service unavailable"
    }
  },
  "theme": {
    "light": "LIGHT",
    "dark": "DARK",
    "ariaCurrent": "Current {{state}} theme, click to switch to {{next}}"
  },
  "lang": {
    "zh": "中",
    "en": "EN",
    "aria": "Switch language, current {{current}}"
  }
}
```

- [ ] **Step 3：验证 JSON 合法**

Run:

```bash
node -e "console.log(JSON.parse(require('fs').readFileSync('client/src/i18n/locales/zh-CN/translation.json', 'utf8')).app.brand)"
node -e "console.log(JSON.parse(require('fs').readFileSync('client/src/i18n/locales/en/translation.json', 'utf8')).app.brand)"
```

Expected: 两行都打印 `my-ai`

- [ ] **Step 4：commit**

```bash
git add client/src/i18n/locales/zh-CN/translation.json client/src/i18n/locales/en/translation.json
git commit -m "feat(v5): 新增 i18n 资源文件（zh-CN + en）"
```

---

### Task 4：新增 `client/src/i18n/index.ts`（i18next init）

**Files:**

- Create: `client/src/i18n/index.ts`
- Create: `client/src/i18n/index.test.ts`

- [ ] **Step 1：写 i18n/index.test.ts 失败测试**

创建 `client/src/i18n/index.test.ts`：

```ts
// i18n init 行为测试（v5）：覆盖 localStorage / navigator / fallback 三个降级层。
// 测试 setup 钉死 zh-CN（client/test/setup.ts 顶部 i18n.changeLanguage('zh-CN')），
// 本文件主要验证 detectLng 行为；运行时 i18n.language 已被 setup 改写。
import { describe, it, expect, beforeEach } from 'vitest';

describe('i18n init', () => {
  beforeEach(() => {
    // 每次测试前清空 localStorage 与 navigator.language
    try {
      localStorage.clear();
    } catch {
      // 隐私模式可能不可用
    }
  });

  it('localStorage 缺 + navigator=en-US → init 后 i18n.language 含 en', async () => {
    Object.defineProperty(navigator, 'language', { value: 'en-US', configurable: true });
    const { default: i18n } = await import('./index.js?v=nolang&' + Date.now());
    expect(i18n.language).toBe('en');
  });

  it('localStorage 缺 + navigator=zh-CN → init 后 i18n.language=zh-CN', async () => {
    Object.defineProperty(navigator, 'language', { value: 'zh-CN', configurable: true });
    const { default: i18n } = await import('./index.js?v=nolangzh&' + Date.now());
    expect(i18n.language).toBe('zh-CN');
  });

  it('localStorage 缺 + navigator=ja-JP → 降级 zh-CN', async () => {
    Object.defineProperty(navigator, 'language', { value: 'ja-JP', configurable: true });
    const { default: i18n } = await import('./index.js?v=nolangja&' + Date.now());
    expect(i18n.language).toBe('zh-CN');
  });

  it('i18n.t 翻译 zh-CN 资源能拿到中文文案', async () => {
    const { default: i18n } = await import('./index.js?v=t1&' + Date.now());
    expect(i18n.t('settings.test', { lng: 'zh-CN' })).toBe('测试');
  });

  it('i18n.t 翻译 en 资源能拿到英文文案', async () => {
    const { default: i18n } = await import('./index.js?v=t2&' + Date.now());
    expect(i18n.t('settings.test', { lng: 'en' })).toBe('Test');
  });
});
```

- [ ] **Step 2：跑测试看它失败（应找不到 index.js）**

Run:

```bash
pnpm --filter @my-ai/client run test -- index.test
```

Expected: FAIL with "Cannot find module './index.js'" 或 "Failed to resolve import"

- [ ] **Step 3：写 i18n/index.ts**

创建 `client/src/i18n/index.ts`：

```ts
// 客户端 i18n 初始化（v5）：静态 import 资源 + 检测链。
// 资源文件小（en + zh-CN 合计 < 5KB），无 HTTP 开销、无 CORS 顾虑（Tauri file:// 协议）。
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import en from './locales/en/translation.json';
import zhCN from './locales/zh-CN/translation.json';

const STORAGE_KEY = 'my-ai:lang';
const SUPPORTED = ['zh-CN', 'en'] as const;
export type Supported = (typeof SUPPORTED)[number];

function detectLng(): Supported {
  // 1. localStorage
  try {
    const stored = localStorage.getItem(STORAGE_KEY);
    if (stored && (SUPPORTED as readonly string[]).includes(stored)) {
      return stored as Supported;
    }
  } catch {
    // 隐私模式 / localStorage 不可用 → 降级
  }
  // 2. navigator.language 主语种
  const primary = typeof navigator !== 'undefined' ? (navigator.language.split('-')[0] ?? '') : '';
  if (primary === 'zh') {
    return 'zh-CN';
  }
  if (primary === 'en') {
    return 'en';
  }
  // 3. fallback
  return 'zh-CN';
}

i18n
  .use(initReactI18next)
  .init({
    resources: {
      en: { translation: en },
      'zh-CN': { translation: zhCN },
    },
    lng: detectLng(),
    fallbackLng: 'zh-CN',
    interpolation: {
      escapeValue: false, // React 已防 XSS，i18next 不必再 escape
    },
    returnNull: false, // 缺 key 时返回 fallbackLng 串，不返回空串
  })
  .catch((e: unknown) => {
    // i18n init 失败时只 log，不阻塞应用启动
    console.error('i18n init failed:', e);
  });

export { SUPPORTED };
export default i18n;
```

- [ ] **Step 4：跑测试看它通过**

Run:

```bash
pnpm --filter @my-ai/client run test -- index.test
```

Expected: 5 个测试全过

如有失败：检查 `en.json` / `zh-CN.json` 是否 JSON 损坏；检查 `detectLng` 逻辑。

- [ ] **Step 5：跑全 client 测试 + typecheck 确认无回归**

Run:

```bash
pnpm --filter @my-ai/client run typecheck && pnpm --filter @my-ai/client run test
```

Expected: typecheck Done；既有 11 个测试文件全过（i18n init 是新增文件不影响既有）

- [ ] **Step 5.5：在 client/src/main.tsx 加 i18n init 的 side-effect import**

打开 `client/src/main.tsx`，在文件顶部 import 区（任何 import 之后）加：

```ts
// v5: 触发 i18n 初始化（side-effect import）
import './i18n/index.js';
```

`i18n.init` 内部 `.catch` 兜底，main.tsx 同步代码不需 await。React 渲染时 `i18n.language` 已确定。

可选验证（不进 CI 必跑，仅 dev 阶段确认）：

```bash
pnpm --filter @my-ai/client run build
```

Expected: Vite 成功 bundle i18n 模块，dist 产物含 en.json + zh-CN.json 资源。

- [ ] **Step 6：commit**

```bash
git add client/src/i18n/index.ts client/src/i18n/index.test.ts
git commit -m "feat(v5): 新增 i18n init（检测链 + 静态 import 资源）"
```

---

### Task 5：测试 setup 钉死 zh-CN

**Files:**

- Modify: `client/test/setup.ts`

- [ ] **Step 1：读现有 setup.ts**

Run:

```bash
cat client/test/setup.ts
```

如文件不存在，跳过此 task；继续 Task 6。如存在（v4 状态应有），继续 Step 2。

- [ ] **Step 2：在 setup.ts 顶部加 i18n.changeLanguage('zh-CN')**

打开 `client/test/setup.ts`，在 import 之后、其他代码之前加：

```ts
// v5: 钉死测试语种为 zh-CN，避免依赖 navigator.language
import i18n from '@/i18n/index.js';
i18n.changeLanguage('zh-CN');
```

注意 import path 用 v4 起的 `@/` alias；这个 import 会触发 `client/src/i18n/index.ts` 模块加载（即 i18n init 的 side-effect），所以**不需要**在 vitest setup 之外再 import i18n。

- [ ] **Step 3：跑 client 测试 + typecheck**

Run:

```bash
pnpm --filter @my-ai/client run typecheck && pnpm --filter @my-ai/client run test
```

Expected: typecheck Done；所有测试全过（既有 11 个 + Task 4 加的 5 个）

如失败：检查 `@/i18n/index.js` 是否能在 vitest jsdom 环境 resolve（v4 已配 vitest `resolve.alias`）。

- [ ] **Step 4：commit**

```bash
git add client/test/setup.ts
git commit -m "test(v5): 测试 setup 钉死 zh-CN"
```

---

### Task 6：重构 `MismatchBanner` 用 `t()`

**Files:**

- Modify: `client/src/components/MismatchBanner.tsx`
- Modify: `client/test/components/MismatchBanner.test.tsx`

- [ ] **Step 1：读现有测试**

Run:

```bash
cat client/test/components/MismatchBanner.test.tsx
```

按现有断言写 Step 2 改写。

- [ ] **Step 2：把测试断言改 i18n.t(key)**

打开 `client/test/components/MismatchBanner.test.tsx`，把硬编码中文断言改为 `i18n.t(key)`：

- 在文件顶部 import 区加 `import i18n from '@/i18n/index.js';`（如果尚未有 i18n import）
- 把 `'Mismatch'` / `'关闭'` / `'Gateway ... 超出 client 兼容范围 ...'` 之类断言改用 `i18n.t('mismatch.tag')` / `i18n.t('mismatch.dismiss')` / `i18n.t('mismatch.message', { version: '...', range: '...' })`

如果原测试用 `screen.getByText('Mismatch')`，改 `screen.getByText(i18n.t('mismatch.tag'))`。
如果用 `screen.getByText(/关闭/)`，改 `screen.getByText(i18n.t('mismatch.dismiss'))`。

具体改写参考：

```tsx
import i18n from '@/i18n/index.js';

it('显示 tag + 关闭按钮', () => {
  render(
    <MismatchBanner gatewayVersion="1.5.0" requiredRange=">=0.0.3 <0.1.0" onDismiss={() => {}} />,
  );
  expect(screen.getByText(i18n.t('mismatch.tag'))).toBeInTheDocument();
  expect(screen.getByText(i18n.t('mismatch.dismiss'))).toBeInTheDocument();
});

it('message 含 version 与 range 插值', () => {
  render(
    <MismatchBanner gatewayVersion="1.5.0" requiredRange=">=0.0.3 <0.1.0" onDismiss={() => {}} />,
  );
  const expected = i18n.t('mismatch.message', { version: '1.5.0', range: '>=0.0.3 <0.1.0' });
  expect(screen.getByText(expected)).toBeInTheDocument();
});
```

- [ ] **Step 3：跑测试看它失败（实现未改）**

Run:

```bash
pnpm --filter @my-ai/client run test -- MismatchBanner
```

Expected: FAIL with "Unable to find an element with the text: ..." 或 "Mismatch" 字样

- [ ] **Step 4：重构 MismatchBanner.tsx 用 t()**

打开 `client/src/components/MismatchBanner.tsx`，全文案改 `useTranslation` + `t()`：

```tsx
// Mismatch 警告横幅。
// 关闭由父组件的 bannerDismissed state 控制；本组件只负责渲染 + 触发 onDismiss。
// v5: 全文案走 i18n。
import { useTranslation } from 'react-i18next';

type Props = {
  gatewayVersion: string | null;
  requiredRange: string;
  onDismiss: () => void;
};

export function MismatchBanner({ gatewayVersion, requiredRange, onDismiss }: Props) {
  const { t } = useTranslation();
  return (
    <div role="alert" className="mismatch-banner">
      <span>
        <span className="tag">{t('mismatch.tag')}</span>
        {t('mismatch.message', { version: gatewayVersion ?? '', range: requiredRange })}
      </span>
      <button type="button" className="btn" onClick={onDismiss}>
        {t('mismatch.dismiss')}
      </button>
    </div>
  );
}
```

- [ ] **Step 5：跑测试看它通过**

Run:

```bash
pnpm --filter @my-ai/client run test -- MismatchBanner
```

Expected: 全过

- [ ] **Step 6：跑 typecheck**

Run:

```bash
pnpm --filter @my-ai/client run typecheck
```

Expected: Done

- [ ] **Step 7：commit**

```bash
git add client/src/components/MismatchBanner.tsx client/test/components/MismatchBanner.test.tsx
git commit -m "feat(v5): MismatchBanner 文案 i18n 化"
```

---

### Task 7：重构 `PairBanner` 用 `t()`

**Files:**

- Modify: `client/src/components/PairBanner.tsx`
- Modify: `client/test/components/PairBanner.test.tsx`

- [ ] **Step 1：把测试断言改 i18n.t(key)**

打开 `client/test/components/PairBanner.test.tsx`：

- 顶部加 `import i18n from '@/i18n/index.js';`
- 把 `screen.getByText(/未配对/)` → `screen.getByText(i18n.t('pair.banner.needPair.kicker'))` 或 `screen.getByText(i18n.t('pair.banner.needPair.message'))`（视测试断言的具体目标而定）
- `screen.getByText('去配对')` → `screen.getByText(i18n.t('pair.banner.actions.goPair'))`
- `screen.getByText('清除配对')` → `screen.getByText(i18n.t('pair.banner.actions.clear'))`
- NEED_REPAIR case 同理：`/重新配对/` → `i18n.t('pair.banner.needRepair.kicker')` 之类

参考 v5 §5.3 资源结构。

- [ ] **Step 2：跑测试看它失败**

Run:

```bash
pnpm --filter @my-ai/client run test -- PairBanner
```

Expected: FAIL

- [ ] **Step 3：重构 PairBanner.tsx**

```tsx
// 顶部 banner：显示未配对 / 需重新配对 状态 + 提供两个按钮。
// PAIRED / PAIR_PENDING 等其他状态不渲染。
// v5: 全文案走 i18n。
import { useTranslation } from 'react-i18next';

type Variant = 'NEED_PAIR' | 'NEED_REPAIR' | 'PAIRED';

type Props = {
  variant: Variant;
  onGoToPair: () => void;
  onClear: () => void;
};

export function PairBanner({ variant, onGoToPair, onClear }: Props) {
  const { t } = useTranslation();
  if (variant === 'PAIRED') {
    return null;
  }
  const isRepair = variant === 'NEED_REPAIR';
  // kicker / message 来自 i18n；不同 variant 用不同 namespace
  const ns = isRepair ? 'pair.banner.needRepair' : 'pair.banner.needPair';
  const num = isRepair ? '01' : '00';
  return (
    <div role="alert" className={isRepair ? 'banner banner--need-repair' : 'banner'}>
      <span className="banner-num" aria-hidden="true">
        {num}
      </span>
      <span className="banner-text">
        <strong>{t(`${ns}.kicker`)}</strong>
        {t(`${ns}.message`)}
      </span>
      <span className="banner-actions">
        <button type="button" className="btn btn--primary" onClick={onGoToPair}>
          {t('pair.banner.actions.goPair')}
        </button>
        <button type="button" className="btn" onClick={onClear}>
          {t('pair.banner.actions.clear')}
        </button>
      </span>
    </div>
  );
}
```

- [ ] **Step 4：跑测试 + typecheck 全过**

Run:

```bash
pnpm --filter @my-ai/client run typecheck && pnpm --filter @my-ai/client run test -- PairBanner
```

Expected: 全过

- [ ] **Step 5：commit**

```bash
git add client/src/components/PairBanner.tsx client/test/components/PairBanner.test.tsx
git commit -m "feat(v5): PairBanner 文案 i18n 化"
```

---

### Task 8：重构 `Settings` 用 `t()`

**Files:**

- Modify: `client/src/components/Settings.tsx`
- Modify: `client/test/components/Settings.test.tsx`

- [ ] **Step 1：把测试断言改 i18n.t(key)**

打开 `client/test/components/Settings.test.tsx`：

- 顶部加 `import i18n from '@/i18n/index.js';`
- `screen.getByRole('button', { name: /测试/ })` → `screen.getByRole('button', { name: i18n.t('settings.test') })`
- `screen.getByText('网关')` → `screen.getByText(i18n.t('settings.gatewayLabel'))`
- 4 种 status 的断言：原本可能 mock 不同 status 看 STATUS_LABEL 渲染；改用 `i18n.t('settings.status.<STATUS>')`
  - `'正在测试…'` → `i18n.t('settings.status.PAIRING')`
  - `'配对成功'` → `i18n.t('settings.status.HEALTHY')`
  - `'版本不匹配'` → `i18n.t('settings.status.MISMATCH')`
  - `'连接失败'` → `i18n.t('settings.status.PAIR_FAILED')`
- 'STATUS / GATEWAY' / 'Gateway 状态' 之类若断言存在 → 改 `i18n.t('settings.statusHead')` / `i18n.t('settings.label')`

参考 v5 §5.3 + 现有 Settings.tsx 的 STATUS_LABEL 用途。

- [ ] **Step 2：跑测试看它失败**

Run:

```bash
pnpm --filter @my-ai/client run test -- Settings
```

Expected: FAIL

- [ ] **Step 3：重构 Settings.tsx**

```tsx
// Gateway 配对面板：URL 输入 + 测试按钮 + 状态指示器。
// 纯展示组件，所有状态都由父组件（App.tsx）持有与调度。
// v5: 全文案走 i18n；STATUS_LABEL 从 module-level Record 改运行时 t() 调用。
// 文案 / aria-label 保持向后兼容（被 Settings.test.tsx 断言）。
import { useTranslation } from 'react-i18next';
import type { HandshakeStatus } from '../compat/handshake.js';

type Props = {
  url: string;
  onUrlChange: (next: string) => void;
  onTest: () => void;
  status: HandshakeStatus;
  version: string | null;
};

const STATUS_OK: Record<HandshakeStatus, 'true' | 'false' | 'warn' | 'pending'> = {
  PAIRING: 'pending',
  HEALTHY: 'true',
  MISMATCH: 'warn',
  PAIR_FAILED: 'false',
};

export function Settings({ url, onUrlChange, onTest, status, version }: Props) {
  const { t } = useTranslation();
  // 仅在拿得到 version 的两种状态下追加 gateway 版本号。
  const showVersion = (status === 'HEALTHY' || status === 'MISMATCH') && version;
  return (
    <section className="panel" aria-label={t('settings.label')}>
      <header className="panel-head">
        <span>{t('settings.statusHead')}</span>
        <span className="panel-num">02</span>
      </header>
      <div className="panel-body">
        <div className="panel-status">
          <span className="dot" data-ok={STATUS_OK[status]} aria-hidden="true" />
          <span className="status-label">{t('settings.gatewayLabel')}</span>
          <span className="status-value" data-ok={STATUS_OK[status]}>
            {t(`settings.status.${status}`)}
          </span>
        </div>
        {showVersion ? (
          <span className="status-version">
            {t('settings.versionPrefix')} <code>v{version}</code>
          </span>
        ) : null}
        <div className="url-row">
          <input
            type="text"
            className="input"
            value={url}
            onChange={e => onUrlChange(e.target.value)}
            placeholder="http://gateway-host:8787"
            aria-label={t('settings.urlAria')}
          />
          <button type="button" className="btn" onClick={onTest}>
            {t('settings.test')}
          </button>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 4：跑测试 + typecheck 全过**

Run:

```bash
pnpm --filter @my-ai/client run typecheck && pnpm --filter @my-ai/client run test -- Settings
```

Expected: 全过

- [ ] **Step 5：commit**

```bash
git add client/src/components/Settings.tsx client/test/components/Settings.test.tsx
git commit -m "feat(v5): Settings 文案 + STATUS_LABEL i18n 化"
```

---

### Task 9：重构 `PairDialog` 用 `t()`（含错误码 map）

**Files:**

- Modify: `client/src/components/PairDialog.tsx`
- Modify: `client/test/components/PairDialog.test.tsx`

- [ ] **Step 1：把测试断言改 i18n.t(key)**

打开 `client/test/components/PairDialog.test.tsx`：

- 顶部加 `import i18n from '@/i18n/index.js';`
- 现有测试断言里的硬编码中文（'取消' / '提交' / '正在连接网关…' / '正在配对…' / '私有模式凭证' / 'alice-laptop' / 'REQ' / 'OPT' / '客户端名' / 'Pair Key' 之类）全部改 `i18n.t('pair.dialog...')` 形式
- aria-label 断言（'Gateway URL' / 'Pair Key (可选)' / '客户端名'）改 `i18n.t('pair.dialog.fields.<field>.<aria>')` 或 `.label`（视断言而定）

参考 v5 §5.3 PairDialog namespace。

- [ ] **Step 2：跑测试看它失败**

Run:

```bash
pnpm --filter @my-ai/client run test -- PairDialog
```

Expected: FAIL

- [ ] **Step 3：重构 PairDialog.tsx**

```tsx
// 配对弹出层：表单 + 提交 → GET /health 检查连通 → POST /pair → 成功/轮询。
// v5: 全文案走 i18n；错误码 map（friendlyApiError 内 Record<number,string>）改为
//     资源文件查表 i18n.t('errors.api.<code>')。
// 文案 / aria-label / role 保持向后兼容（被 PairDialog.test.tsx 断言）。
import { useState } from 'react';
import { useTranslation } from 'react-i18next';
import { apiFetch, ApiError } from '../lib/api.js';
import { saveSecureConfig } from '../lib/secure-store.js';

type Props = {
  initialUrl: string;
  initialPairKey?: string | null;
  initialName?: string | null;
  clientKey: string;
  onPaired: (info: {
    clientKey: string;
    name: string | null;
    gatewayUrl: string;
    pairKey: string | null;
  }) => void;
  onClose: () => void;
};

const POLL_INTERVAL_MS = 2000;
const POLL_TIMEOUT_MS = 5 * 60 * 1000;

// 网络层错误（fetch 失败 / JSON 解析失败）→ 普通用户能看懂的提示。
// v5: 文案从字面量改查 i18n key。
function friendlyNetworkError(e: unknown, prefix: string, t: (k: string) => string): string {
  const raw = e instanceof Error ? e.message : String(e);
  const msg = raw && raw !== 'undefined' && raw !== 'null' ? raw : '';
  if (
    msg === 'Failed to fetch' ||
    msg === 'Load failed' ||
    msg === 'NetworkError' ||
    msg === 'Network request failed'
  ) {
    return `${prefix}：${t('errors.network.fetchFail')}`;
  }
  if (msg.startsWith('invalid JSON response')) {
    return `${prefix}：${t('errors.network.notMyAi')}`;
  }
  if (msg) {
    return `${prefix}：${msg}`;
  }
  return `${prefix}：${t('errors.network.unknown')}`;
}

// 网关业务码 → 普通用户能看懂的提示。code 来自 gateway/src/response.ts err()。
// v5: 文案从字面量 Record 改为查 i18n key；未列出 code 走 defaultValue + 兜底。
function friendlyApiError(
  e: unknown,
  prefix: string,
  t: (k: string, o?: Record<string, string>) => string,
): string {
  if (!(e instanceof ApiError)) {
    return friendlyNetworkError(e, prefix, t);
  }
  const translated = t(`errors.api.${e.code}`);
  if (translated && translated !== `errors.api.${e.code}`) {
    return `${prefix}：${translated}`;
  }
  // 兜底：message 可能为 undefined（网关漏字段）或空字符串
  const raw = (e.message ?? '').toString();
  if (raw && raw !== 'undefined' && raw !== 'null') {
    return `${prefix}：${raw}`;
  }
  return `${prefix}：错误码 ${e.code}`;
}

// 复制文本到剪贴板，1.5s 内反馈 'copied' 后回 'idle'。
async function copyCmd(text: string, setCopied: (s: 'idle' | 'copied') => void): Promise<void> {
  try {
    await navigator.clipboard.writeText(text);
    setCopied('copied');
    setTimeout(() => setCopied('idle'), 1500);
  } catch {
    // ignore：用户可手动选中文本复制
  }
}

export function PairDialog({
  initialUrl,
  initialPairKey = null,
  initialName = null,
  clientKey,
  onPaired,
  onClose,
}: Props) {
  const { t } = useTranslation();
  const [url, setUrl] = useState(initialUrl);
  const [pairKey, setPairKey] = useState(initialPairKey ?? '');
  const [name, setName] = useState(initialName ?? '');
  const [error, setError] = useState<string | null>(null);
  const [status, setStatus] = useState<string | null>(null);
  const [statusState, setStatusState] = useState<'idle' | 'pending'>('idle');
  const [token, setToken] = useState<string | null>(null);
  const [copied, setCopied] = useState<'idle' | 'copied'>('idle');

  const submit = async () => {
    setError(null);
    setStatus(t('pair.dialog.status.connecting'));
    setStatusState('idle');
    try {
      await apiFetch(`${url}/health`, { clientKey });
    } catch (e) {
      setError(friendlyNetworkError(e, t('pair.dialog.errors.network'), t));
      setStatus(null);
      return;
    }
    setStatus(t('pair.dialog.status.pairing'));
    try {
      const data = await apiFetch<{ clientKey: string; name: string | null }>(`${url}/pair`, {
        method: 'POST',
        clientKey,
        body: { clientKey, name: name || null, pairKey: pairKey || undefined },
      });
      await saveSecureConfig({
        clientKey: data.clientKey,
        gatewayUrl: url,
        pairKey: pairKey || null,
        clientName: data.name,
      });
      onPaired({
        clientKey: data.clientKey,
        name: data.name,
        gatewayUrl: url,
        pairKey: pairKey || null,
      });
      return;
    } catch (e) {
      if (e instanceof ApiError && e.code === 0) {
        const tk = (e.data as { token?: string } | null)?.token;
        if (!tk) {
          setError(t('pair.dialog.errors.missingToken'));
          setStatus(null);
          return;
        }
        setToken(tk);
        setStatus(t('pair.dialog.status.pending'));
        setStatusState('pending');
        let stopped = false;
        const poll = async () => {
          if (stopped) {
            return;
          }
          try {
            const r = await apiFetch<{ status: string }>(`${url}/pair/status?token=${tk}`);
            if (r.status === 'PAIRED') {
              stopped = true;
              await saveSecureConfig({
                clientKey,
                gatewayUrl: url,
                pairKey: pairKey || null,
                clientName: name || null,
              });
              onPaired({
                clientKey,
                name: name || null,
                gatewayUrl: url,
                pairKey: pairKey || null,
              });
            }
          } catch (e) {
            if (e instanceof ApiError && e.code === 404) {
              stopped = true;
              try {
                await saveSecureConfig({
                  clientKey,
                  gatewayUrl: url,
                  pairKey: pairKey || null,
                  clientName: name || null,
                });
              } catch (saveErr) {
                console.warn('saveSecureConfig failed (production 应不发生):', saveErr);
              }
              onPaired({
                clientKey,
                name: name || null,
                gatewayUrl: url,
                pairKey: pairKey || null,
              });
            }
          }
        };
        const id = setInterval(poll, POLL_INTERVAL_MS);
        setTimeout(() => {
          stopped = true;
          clearInterval(id);
          if (status === t('pair.dialog.status.pending')) {
            setError(t('pair.dialog.errors.timeout'));
            setStatus(null);
            setStatusState('idle');
          }
        }, POLL_TIMEOUT_MS);
        void poll();
        return;
      }
      setError(friendlyApiError(e, t('pair.dialog.errors.pairFail'), t));
      setStatus(null);
      setStatusState('idle');
    }
  };

  return (
    <div
      className="dialog-backdrop"
      onClick={e => {
        if (e.target === e.currentTarget) {
          onClose();
        }
      }}
    >
      <div role="dialog" aria-label={t('pair.dialog.title')} className="dialog">
        <header className="dialog-head">
          <h2 className="dialog-title">
            {t('pair.dialog.title')}
            <em>·</em>
          </h2>
          <span className="dialog-sub">{t('pair.dialog.sub')}</span>
        </header>
        <div className="dialog-body">
          <div className="field">
            <label className="field-label" htmlFor="pair-url">
              <span>{t('pair.dialog.fields.url.label')}</span>
              <span className="req">{t('pair.dialog.fields.url.req')}</span>
            </label>
            <input
              id="pair-url"
              className="input"
              type="text"
              value={url}
              onChange={e => setUrl(e.target.value)}
              aria-label={t('pair.dialog.fields.url.label')}
              placeholder={t('pair.dialog.fields.url.placeholder')}
              autoComplete="off"
              spellCheck={false}
            />
          </div>
          <div className="field">
            <label className="field-label" htmlFor="pair-key">
              <span>{t('pair.dialog.fields.pairKey.label')}</span>
              <span>{t('pair.dialog.fields.pairKey.opt')}</span>
            </label>
            <input
              id="pair-key"
              className="input"
              type="password"
              value={pairKey}
              onChange={e => setPairKey(e.target.value)}
              aria-label={t('pair.dialog.fields.pairKey.aria')}
              placeholder={t('pair.dialog.fields.pairKey.placeholder')}
              autoComplete="off"
            />
          </div>
          <div className="field">
            <label className="field-label" htmlFor="pair-name">
              <span>{t('pair.dialog.fields.name.label')}</span>
              <span>{t('pair.dialog.fields.name.opt')}</span>
            </label>
            <input
              id="pair-name"
              className="input"
              type="text"
              value={name}
              onChange={e => setName(e.target.value)}
              aria-label={t('pair.dialog.fields.name.aria')}
              placeholder={t('pair.dialog.fields.name.placeholder')}
              autoComplete="off"
            />
          </div>
          {status && (
            <div className="dialog-status" data-state={statusState} role="status">
              {status}
            </div>
          )}
          {token && (
            <div className="field">
              <span className="field-label">
                <span>{t('pair.dialog.token.label')}</span>
                <span>{t('pair.dialog.token.hintHead')}</span>
              </span>
              <code className="token-block">{token}</code>
              {/* CLI 命令原样保留，不进 i18n */}
              <div className="cmd-row">
                <code className="cmd-line">my-ai-gateway pair --token {token}</code>
                <button
                  type="button"
                  className="btn-copy"
                  onClick={() => {
                    void copyCmd(`my-ai-gateway pair --token ${token}`, setCopied);
                  }}
                >
                  {copied === 'copied'
                    ? t('pair.dialog.actions.copied')
                    : t('pair.dialog.actions.copy')}
                </button>
              </div>
              <p className="cmd-hint">
                源码 dev 模式：<code>pnpm --filter @my-ai/gateway run pair -- --token {token}</code>
              </p>
            </div>
          )}
          {error && (
            <div className="dialog-error" role="alert">
              {error}
            </div>
          )}
        </div>
        <footer className="dialog-foot">
          <button type="button" className="btn btn--ghost" onClick={onClose}>
            {t('pair.dialog.actions.cancel')}
          </button>
          <button
            type="button"
            className="btn btn--signal"
            onClick={() => {
              void submit();
            }}
          >
            {t('pair.dialog.actions.submit')}
          </button>
        </footer>
      </div>
    </div>
  );
}
```

- [ ] **Step 4：跑测试 + typecheck 全过**

Run:

```bash
pnpm --filter @my-ai/client run typecheck && pnpm --filter @my-ai/client run test -- PairDialog
```

Expected: 全过

- [ ] **Step 5：commit**

```bash
git add client/src/components/PairDialog.tsx client/test/components/PairDialog.test.tsx
git commit -m "feat(v5): PairDialog 文案 + 错误码 map i18n 化"
```

---

### Task 10：重构 `ThemeToggle` 用 `t()`

**Files:**

- Modify: `client/src/components/ThemeToggle.tsx`
- Modify: `client/test/components/ThemeToggle.test.tsx`

- [ ] **Step 1：把测试断言改 i18n.t(key)**

打开 `client/test/components/ThemeToggle.test.tsx`：

- 顶部加 `import i18n from '@/i18n/index.js';`
- 把 `'LIGHT'` / `'DARK'` / '当前...色主题...' / '主题 · ...' 之类断言改 `i18n.t('theme.light')` / `i18n.t('theme.dark')` / `i18n.t('theme.ariaCurrent', { state: ..., next: ... })`

- [ ] **Step 2：跑测试看它失败**

Run:

```bash
pnpm --filter @my-ai/client run test -- ThemeToggle
```

Expected: FAIL

- [ ] **Step 3：重构 ThemeToggle.tsx**

```tsx
// 主题切换按钮（v5）：明 ↔ 暗。点轮换 + localStorage 持久化。
// v5: 全文案走 i18n（aria-label 含 state/next 插值）。
import { useCallback, useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';

export type Theme = 'light' | 'dark';
const STORAGE_KEY = 'my-ai:theme';
const DEFAULT_THEME: Theme = 'light';

function readInitialTheme(): Theme {
  if (typeof window === 'undefined') {
    return DEFAULT_THEME;
  }
  try {
    const stored = window.localStorage.getItem(STORAGE_KEY);
    if (stored === 'light' || stored === 'dark') {
      return stored;
    }
  } catch {
    // ignore
  }
  if (window.matchMedia?.('(prefers-color-scheme: dark)').matches) {
    return 'dark';
  }
  return DEFAULT_THEME;
}

function applyTheme(theme: Theme): void {
  document.documentElement.setAttribute('data-theme', theme);
}

export function ThemeToggle() {
  const { t } = useTranslation();
  const [theme, setTheme] = useState<Theme>(readInitialTheme);

  useEffect(() => {
    applyTheme(theme);
    try {
      window.localStorage.setItem(STORAGE_KEY, theme);
    } catch {
      // ignore
    }
  }, [theme]);

  const toggle = useCallback(() => {
    setTheme(prev => (prev === 'dark' ? 'light' : 'dark'));
  }, []);

  const isDark = theme === 'dark';
  const next = isDark ? t('theme.light') : t('theme.dark');
  const stateLabel = isDark ? t('theme.dark') : t('theme.light');
  const nextValue: Theme = isDark ? 'light' : 'dark';
  const nextLabel = isDark ? t('theme.light') : t('theme.dark');

  return (
    <button
      type="button"
      className="theme-toggle"
      data-current={theme}
      onClick={toggle}
      aria-label={t('theme.ariaCurrent', { state: stateLabel, next: nextLabel })}
      title={`${stateLabel} → ${nextValue}`}
    >
      <span className="dot" aria-hidden="true" />
      <span>{next}</span>
    </button>
  );
}
```

- [ ] **Step 4：跑测试 + typecheck 全过**

Run:

```bash
pnpm --filter @my-ai/client run typecheck && pnpm --filter @my-ai/client run test -- ThemeToggle
```

Expected: 全过

- [ ] **Step 5：commit**

```bash
git add client/src/components/ThemeToggle.tsx client/test/components/ThemeToggle.test.tsx
git commit -m "feat(v5): ThemeToggle 文案 i18n 化"
```

---

### Task 11：新增 `<LanguageSwitcher />` 组件

**Files:**

- Create: `client/src/i18n/switch.tsx`
- Create: `client/src/i18n/switch.test.tsx`

- [ ] **Step 1：写 switch.test.tsx 失败测试**

创建 `client/src/i18n/switch.test.tsx`：

```tsx
// <LanguageSwitcher /> 行为测试（v5）：切语种 + localStorage 写入。
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import i18n from './index.js';
import { LanguageSwitcher } from './switch.js';

describe('<LanguageSwitcher>', () => {
  beforeEach(() => {
    i18n.changeLanguage('zh-CN');
    try {
      localStorage.clear();
    } catch {
      // ignore
    }
  });

  it('初始渲染当前语言代码（zh-CN → "中"）', () => {
    render(<LanguageSwitcher />);
    expect(screen.getByText(i18n.t('lang.zh'))).toBeInTheDocument();
  });

  it('点按钮 → 切到 en + localStorage 写入 en', async () => {
    render(<LanguageSwitcher />);
    fireEvent.click(screen.getByRole('button'));
    // i18n.changeLanguage 是 async，等 promise 完成
    await i18n.changeLanguage('en');
    expect(localStorage.getItem('my-ai:lang')).toBe('en');
  });

  it('切到 en 后渲染 "EN" 文字', async () => {
    render(<LanguageSwitcher />);
    fireEvent.click(screen.getByRole('button'));
    await i18n.changeLanguage('en');
    expect(screen.getByText(i18n.t('lang.en'))).toBeInTheDocument();
  });

  it('aria-label 含当前语言名', () => {
    render(<LanguageSwitcher />);
    const btn = screen.getByRole('button');
    expect(btn.getAttribute('aria-label')).toContain(i18n.t('lang.zh'));
  });
});
```

- [ ] **Step 2：跑测试看它失败**

Run:

```bash
pnpm --filter @my-ai/client run test -- switch.test
```

Expected: FAIL with "Cannot find module './switch.js'"

- [ ] **Step 3：写 switch.tsx**

创建 `client/src/i18n/switch.tsx`：

```tsx
// 顶栏语言切换按钮（v5）：zh-CN ↔ en。点轮换 + 写 localStorage。
// 与 ThemeToggle 同级（header 内右侧）。2 个语种用按钮最简，不做下拉。
import { useTranslation } from 'react-i18next';
import { SUPPORTED } from './index.js';

const NEXT: Record<string, string> = { 'zh-CN': 'en', en: 'zh-CN' };

export function LanguageSwitcher() {
  const { i18n, t } = useTranslation();
  const current = i18n.language;
  const next = NEXT[current] ?? 'en';
  const nextLabel = t(`lang.${next}`);
  const currentLabel = t(`lang.${current}`);

  const handleClick = () => {
    void i18n.changeLanguage(next).then(() => {
      try {
        localStorage.setItem('my-ai:lang', next);
      } catch {
        // 隐私模式 / localStorage 不可用 → 静默忽略（会话期内有效即可）
      }
    });
  };

  return (
    <button
      type="button"
      className="lang-toggle"
      data-current={current}
      onClick={handleClick}
      aria-label={t('lang.aria', { current: currentLabel })}
      title={`${currentLabel} → ${nextLabel}`}
    >
      <span className="dot" aria-hidden="true" />
      <span>{currentLabel}</span>
    </button>
  );
}

export { SUPPORTED };
```

- [ ] **Step 4：跑测试看它通过**

Run:

```bash
pnpm --filter @my-ai/client run test -- switch.test
```

Expected: 4 个测试全过

- [ ] **Step 5：跑 typecheck**

Run:

```bash
pnpm --filter @my-ai/client run typecheck
```

Expected: Done

- [ ] **Step 6：commit**

```bash
git add client/src/i18n/switch.tsx client/src/i18n/switch.test.tsx
git commit -m "feat(v5): 新增 <LanguageSwitcher /> 组件"
```

---

### Task 12：重构 `App.tsx`（header / footer t() + 加 LanguageSwitcher）

**Files:**

- Modify: `client/src/App.tsx`

- [ ] **Step 1：读 App.tsx 现状**

Run:

```bash
cat client/src/App.tsx
```

按现有结构改写。注意：v5 阶段 `App.tsx` 状态机 / 业务逻辑不变，只动 header h1 / em / 副标题 / footer 文案 + 加 LanguageSwitcher。

- [ ] **Step 2：改写 App.tsx**

```tsx
// 客户端根组件（v5）：状态机 + 配对 banner/dialog + 业务 401 被动感知 + i18n。
// v5: header h1 / em / 副标题 / footer 走 i18n；header 加 <LanguageSwitcher />。
// 状态机：PAIRING → NEED_PAIR / PAIR_PENDING / PAIRED / NEED_REPAIR / PAIR_FAILED。
import { useCallback, useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';
import { handshake, type HandshakeStatus } from './compat/handshake.js';
import { COMPAT } from './compat.generated.js';
import { Settings } from './components/Settings.js';
import { MismatchBanner } from './components/MismatchBanner.js';
import { PairBanner } from './components/PairBanner.js';
import { PairDialog } from './components/PairDialog.js';
import { ThemeToggle } from './components/ThemeToggle.js';
import { LanguageSwitcher } from './i18n/switch.js';
import { loadSecureConfig, clearSecureConfig, type SecureConfig } from './lib/secure-store.js';
import { randomUUID } from './lib/uuid.js';

const GATEWAY_URL = import.meta.env.VITE_GATEWAY_URL ?? 'http://127.0.0.1:8787';
const HEARTBEAT_MS = Number(import.meta.env.VITE_HEARTBEAT_INTERVAL_MS ?? 5 * 60 * 1000);

type AppStatus =
  | 'NEED_PAIR'
  | 'PAIR_PENDING'
  | 'PAIRED'
  | 'NEED_REPAIR'
  | 'PAIR_FAILED'
  | 'PAIRING';

function toHandshakeStatus(s: AppStatus, mismatch: boolean): HandshakeStatus {
  if (s === 'PAIRED') {
    return mismatch ? 'MISMATCH' : 'HEALTHY';
  }
  if (s === 'PAIRING' || s === 'PAIR_PENDING') {
    return 'PAIRING';
  }
  return 'PAIR_FAILED';
}

function App() {
  const { t } = useTranslation();
  const [status, setStatus] = useState<AppStatus>('PAIRING');
  const [version, setVersion] = useState<string | null>(null);
  const [isMismatch, setIsMismatch] = useState(false);
  const [bannerDismissed, setBannerDismissed] = useState(false);
  const [secureConfig, setSecureConfig] = useState<SecureConfig | null>(null);
  const [showDialog, setShowDialog] = useState(false);
  const [draftKey, setDraftKey] = useState<string>(() => randomUUID());

  useEffect(() => {
    let cancelled = false;
    void loadSecureConfig()
      .then(cfg => {
        if (cancelled) {
          return;
        }
        if (cfg) {
          setSecureConfig(cfg);
        } else {
          setStatus('NEED_PAIR');
        }
      })
      .catch(() => {
        if (!cancelled) {
          setStatus('NEED_PAIR');
        }
      });
    return () => {
      cancelled = true;
    };
  }, []);

  const runHandshake = useCallback(async (cfg: SecureConfig) => {
    const next = await handshake(cfg.gatewayUrl, COMPAT, cfg.clientKey);
    if (next.status === 'HEALTHY') {
      setStatus('PAIRED');
      setIsMismatch(false);
      setVersion(next.version);
    } else if (next.status === 'MISMATCH') {
      setStatus('PAIRED');
      setIsMismatch(true);
      setVersion(next.version);
    } else if (next.status === 'PAIR_FAILED') {
      setStatus('NEED_REPAIR');
    }
  }, []);

  useEffect(() => {
    if (!secureConfig) {
      return;
    }
    let cancelled = false;
    const run = async () => {
      if (cancelled) {
        return;
      }
      await runHandshake(secureConfig);
    };
    void run();
    const id = setInterval(run, HEARTBEAT_MS);
    return () => {
      cancelled = true;
      clearInterval(id);
    };
  }, [secureConfig, runHandshake]);

  useEffect(() => {
    const onUnauthorized = () => {
      setStatus('NEED_REPAIR');
      setShowDialog(true);
    };
    window.addEventListener('my-ai:unauthorized', onUnauthorized);
    return () => window.removeEventListener('my-ai:unauthorized', onUnauthorized);
  }, []);

  const handlePaired = (info: {
    clientKey: string;
    name: string | null;
    gatewayUrl: string;
    pairKey: string | null;
  }) => {
    setSecureConfig(prev => ({
      clientKey: info.clientKey,
      gatewayUrl: info.gatewayUrl || prev?.gatewayUrl || GATEWAY_URL,
      pairKey: info.pairKey ?? prev?.pairKey ?? null,
      clientName: info.name,
    }));
    setStatus('PAIRED');
    setIsMismatch(false);
    setShowDialog(false);
    setDraftKey(randomUUID());
  };

  const handleClear = async () => {
    await clearSecureConfig();
    setSecureConfig(null);
    setStatus('NEED_PAIR');
    setVersion(null);
    setIsMismatch(false);
    setBannerDismissed(false);
  };

  const settingsStatus = toHandshakeStatus(status, isMismatch);

  const handleTest = () => {
    if (secureConfig) {
      setStatus('PAIRING');
      void runHandshake(secureConfig);
    } else {
      setShowDialog(true);
    }
  };

  return (
    <main className="app">
      <header className="app-meta">
        <span className="brand">{t('app.brand')}</span>
        <ThemeToggle />
        <LanguageSwitcher />
      </header>

      <h1 className="app-title">
        {t('app.title')} <em>{t('app.titleEm')}</em>
      </h1>
      <p className="app-sub">
        <span className="num">v5</span>
        <span>{t('app.subtitle.line1')}</span>
        <span>{t('app.subtitle.sep')}</span>
        <span>{t('app.subtitle.line2', { version: '0.0.4' })}</span>
      </p>

      {(status === 'NEED_PAIR' || status === 'NEED_REPAIR') && (
        <PairBanner
          variant={status}
          onGoToPair={() => setShowDialog(true)}
          onClear={() => {
            void handleClear();
          }}
        />
      )}
      {showDialog && (
        <PairDialog
          initialUrl={secureConfig?.gatewayUrl ?? GATEWAY_URL}
          initialPairKey={secureConfig?.pairKey ?? null}
          initialName={secureConfig?.clientName ?? null}
          clientKey={secureConfig?.clientKey ?? draftKey}
          onPaired={handlePaired}
          onClose={() => setShowDialog(false)}
        />
      )}
      <Settings
        url={secureConfig?.gatewayUrl ?? GATEWAY_URL}
        onUrlChange={() => {
          /* v3 阶段不在 Settings 编辑 URL；由 PairDialog 维护 */
        }}
        onTest={handleTest}
        status={settingsStatus}
        version={version}
      />
      {status === 'PAIRED' && isMismatch && !bannerDismissed && version && (
        <MismatchBanner
          gatewayVersion={version}
          requiredRange={COMPAT.upstream.gateway}
          onDismiss={() => setBannerDismissed(true)}
        />
      )}

      <footer className="app-foot">
        <span>{t('app.footer.copy')}</span>
        <span className="ascii">{t('app.footer.date', { date: '2026/06/09' })}</span>
      </footer>
    </main>
  );
}

export default App;
```

注意：

- `t('app.title')` 返回 "GATEWAY PAIR"，与之前硬编码的 `<h1>GATEWAY <em>PAIR</em></h1>` 拆分：v5 用 `<h1>{t('app.title')} <em>{t('app.titleEm')}</em></h1>`，**输出**仍是 "GATEWAY PAIR"（因为 en / zh-CN 的 app.title 都为 "GATEWAY PAIR"，app.titleEm 都为 "PAIR"）。如需在 zh-CN 中显示 "网关 配对"，需修改 zh-CN 资源（v5 阶段保持"中英混排"风格延续，与 v3/v4 一致）。
- 副标题 `v3` → `v5`（v5 client），`CLIENT 0.0.3` → `CLIENT 0.0.4`（version bump 后）
- footer `━━ 2026/06/08` → `━━ 2026/06/09`（v5 release 日期）

- [ ] **Step 3：跑全 client 测试 + typecheck**

Run:

```bash
pnpm --filter @my-ai/client run typecheck && pnpm --filter @my-ai/client run test
```

Expected: typecheck Done；所有测试全过（含 App.test.tsx 现有 10 个 + 5 个 i18n 初始化 + 4 个 switch + 5 个组件 i18n 化）

如有失败：先看是不是 test 用了 i18n.t(key) 但 key 不存在（回头检查资源文件）；再检查 t() 调用形式。

- [ ] **Step 4：跑全工作区测试 + typecheck 兜底**

Run:

```bash
pnpm -r typecheck && pnpm -r test
```

Expected: 三个子项目全过

- [ ] **Step 5：commit**

```bash
git add client/src/App.tsx
git commit -m "feat(v5): App.tsx 头/尾文案 i18n 化 + 加 <LanguageSwitcher />"
```

---

### Task 13：检查 `App.test.tsx`（review 决定是否改）

**Files:**

- Read: `client/test/App.test.tsx`
- Possibly Modify: `client/test/App.test.tsx`

- [ ] **Step 1：读 App.test.tsx 现状**

Run:

```bash
cat client/test/App.test.tsx
```

- [ ] **Step 2：判断测试是否强耦合中文文案**

检查测试断言：

- 是否有 `screen.getByText(/未配对/)` / `screen.getByText('测试')` / `screen.getByRole('button', { name: /重新配对/ })` 等硬编码中文匹配？
- 还是仅断言 role / attr / 状态（如 "need pair banner appears"）？

- [ ] **Step 3a：若硬耦合中文 → 改 i18n.t(key)**

把硬编码中文断言改为 `i18n.t(key)`，参考 Task 6-10 的改写规则。

例如：

```tsx
// 改前
expect(screen.getByText(/未配对/)).toBeInTheDocument();
// 改后
expect(screen.getByText(i18n.t('pair.banner.needPair.message'))).toBeInTheDocument();
```

注意 import：`import i18n from '@/i18n/index.js';`

- [ ] **Step 3b：若未硬耦合 → 不改**

跳到 Step 4。v3 现状的 App.test.tsx 主要断言 role / banner 出现 / dialog 打开 / heartbeat，未强耦合中文文案。

- [ ] **Step 4：跑全 client 测试 + typecheck**

Run:

```bash
pnpm --filter @my-ai/client run typecheck && pnpm --filter @my-ai/client run test
```

Expected: 全过

- [ ] **Step 5：若改了 → commit；若没改 → 不 commit**

```bash
git add client/test/App.test.tsx
git diff --cached --quiet || git commit -m "test(v5): App.test.tsx 中文断言改 i18n.t(key)"
```

如未改动：

```bash
echo "App.test.tsx 未改，跳过 commit"
```

---

### Task 14：新增 `scripts/check-i18n.mjs` + 测试

**Files:**

- Create: `scripts/check-i18n.mjs`
- Create: `scripts/check-i18n.test.mjs`

- [ ] **Step 1：看现有 scripts 目录**

Run:

```bash
ls scripts/
```

确认风格（v2 起有 `sync-compat.mjs` + `sync-compat.test.mjs` / `check-compat-matrix.mjs` + `check-compat-matrix.test.mjs`，vitest 风格）。

- [ ] **Step 2：写 check-i18n.test.mjs 失败测试**

创建 `scripts/check-i18n.test.mjs`：

```js
// v5: check-i18n 脚本单测。
// 用 vitest 跑；规则：
//  1. en + zh-CN 都是合法 JSON
//  2. key 集合完全一致
//  3. value 必须是 string
import { describe, it, expect } from 'vitest';
import { checkI18n } from './check-i18n.mjs';

describe('check-i18n', () => {
  it('两个 locale 都合法 + key 一致 + value 都是 string → pass', () => {
    const result = checkI18n({
      en: { greeting: 'Hello', farewell: 'Bye' },
      'zh-CN': { greeting: '你好', farewell: '再见' },
    });
    expect(result.ok).toBe(true);
    expect(result.errors).toEqual([]);
  });

  it('en 缺 key → fail', () => {
    const result = checkI18n({
      en: { greeting: 'Hello' },
      'zh-CN': { greeting: '你好', farewell: '再见' },
    });
    expect(result.ok).toBe(false);
    expect(result.errors.some(e => e.includes('farewell'))).toBe(true);
  });

  it('value 不是 string → fail', () => {
    const result = checkI18n({
      en: { greeting: 'Hello', count: 5 },
      'zh-CN': { greeting: '你好', count: 5 },
    });
    expect(result.ok).toBe(false);
    expect(result.errors.some(e => e.includes('count') && e.includes('string'))).toBe(true);
  });

  it('深层嵌套 key 不一致 → fail', () => {
    const result = checkI18n({
      en: { pair: { banner: { kicker: 'A' } } },
      'zh-CN': { pair: { banner: { kicker: 'B', extra: '多余' } } },
    });
    expect(result.ok).toBe(false);
  });

  it('空对象 → pass（key 集合都是空）', () => {
    const result = checkI18n({ en: {}, 'zh-CN': {} });
    expect(result.ok).toBe(true);
  });
});
```

- [ ] **Step 3：跑测试看它失败**

Run:

```bash
pnpm --filter @my-ai/client exec vitest run scripts/check-i18n.test.mjs
```

Expected: FAIL with "Cannot find module './check-i18n.mjs'"

（如果 vitest 不在 client 范围，试试根目录：`pnpm exec vitest run scripts/check-i18n.test.mjs`）

- [ ] **Step 4：写 check-i18n.mjs**

创建 `scripts/check-i18n.mjs`：

```js
// v5: i18n 资源完整性校验（CI 脚本 + 单测共用）。
// 规则：
//  1. en/translation.json 与 zh-CN/translation.json 都是合法 JSON
//  2. 两个文件 key 集合（递归收集路径）完全一致
//  3. 任何 value 不是 string → fail
// 失败码非 0 → CI 红。

import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';

const LOCALES_DIR = resolve('client/src/i18n/locales');
const LOCALES = ['en', 'zh-CN'];

/**
 * 校验给定 in-memory JSON 对象。
 * @param {Record<string, unknown>} data
 * @returns {{ ok: boolean, errors: string[] }}
 */
export function checkI18n(data) {
  const errors = [];

  const flat = {};
  for (const lng of Object.keys(data)) {
    flat[lng] = flattenKeys(data[lng], '', `${lng}/`);
  }

  // 1. value 必须是 string
  for (const lng of Object.keys(flat)) {
    for (const { path, value } of flat[lng]) {
      if (typeof value !== 'string') {
        errors.push(
          `✖ ${lng}${path ? '.' + path : ''} 的 value 不是 string（实际类型 ${typeof value}）`,
        );
      }
    }
  }

  // 2. key 集合完全一致
  const keysByLng = {};
  for (const lng of Object.keys(flat)) {
    keysByLng[lng] = new Set(flat[lng].map(e => e.path));
  }
  const lngs = Object.keys(keysByLng);
  for (let i = 0; i < lngs.length; i++) {
    for (let j = i + 1; j < lngs.length; j++) {
      const a = lngs[i];
      const b = lngs[j];
      const missingA = [...keysByLng[b]].filter(k => !keysByLng[a].has(k));
      const missingB = [...keysByLng[a]].filter(k => !keysByLng[b].has(k));
      for (const k of missingA) {
        errors.push(`✖ ${a} 缺少 key "${k}"（${b} 存在）`);
      }
      for (const k of missingB) {
        errors.push(`✖ ${b} 缺少 key "${k}"（${a} 存在）`);
      }
    }
  }

  return { ok: errors.length === 0, errors };
}

/**
 * 递归收集 key 路径 + value。
 * @param {unknown} obj
 * @param {string} prefix
 * @param {string} label
 * @returns {Array<{ path: string, value: unknown }>}
 */
function flattenKeys(obj, prefix, label) {
  const out = [];
  if (obj === null || obj === undefined || typeof obj !== 'object' || Array.isArray(obj)) {
    if (prefix) {
      out.push({ path: prefix, value: obj });
    }
    return out;
  }
  for (const [k, v] of Object.entries(obj)) {
    const path = prefix ? `${prefix}.${k}` : k;
    if (v !== null && typeof v === 'object' && !Array.isArray(v)) {
      out.push(...flattenKeys(v, path, label));
    } else {
      out.push({ path, value: v });
    }
  }
  return out;
}

// CLI 入口：仅在直接运行时执行
const isMain = import.meta.url === `file://${process.argv[1]}`;
if (isMain) {
  const data = {};
  let loadOk = true;
  for (const lng of LOCALES) {
    const path = resolve(LOCALES_DIR, lng, 'translation.json');
    try {
      const text = readFileSync(path, 'utf8');
      data[lng] = JSON.parse(text);
    } catch (e) {
      console.error(`✖ JSON 解析失败: ${path}\n  ${e.message}`);
      loadOk = false;
    }
  }
  if (!loadOk) {
    process.exit(1);
  }
  const result = checkI18n(data);
  for (const err of result.errors) {
    console.error(err);
  }
  if (result.ok) {
    console.log(`✓ i18n check passed: ${LOCALES.length} locales, key 集合一致`);
  } else {
    process.exit(1);
  }
}
```

- [ ] **Step 5：跑测试看它通过**

Run:

```bash
pnpm exec vitest run scripts/check-i18n.test.mjs
```

Expected: 5 个测试全过

- [ ] **Step 6：跑脚本 CLI 模式（验证真资源文件）**

Run:

```bash
node scripts/check-i18n.mjs
```

Expected: `✓ i18n check passed: 2 locales, key 集合一致`

如失败：检查 en.json 与 zh-CN.json key 集合是否完全对齐（参考 Task 3 的两个 JSON 文件）。

- [ ] **Step 7：commit**

```bash
git add scripts/check-i18n.mjs scripts/check-i18n.test.mjs
git commit -m "ci(v5): 新增 check-i18n 校验脚本（key 集合 + value 类型）"
```

---

### Task 15：新增 `.github/workflows/i18n-check.yml`

**Files:**

- Create: `.github/workflows/i18n-check.yml`

- [ ] **Step 1：读现有同类型 workflow 参考**

Run:

```bash
ls .github/workflows/ && cat .github/workflows/compat-matrix-check.yml
```

按现有风格（v2 §5.12 风格）写新 workflow。

- [ ] **Step 2：写 i18n-check.yml**

创建 `.github/workflows/i18n-check.yml`：

```yaml
name: i18n check
runs-on: ubuntu-latest
steps:
  - uses: actions/checkout@v4
  - uses: pnpm/action-setup@v4
  - uses: actions/setup-node@v4
    with:
      node-version: 20
      cache: pnpm
  - run: pnpm install --frozen-lockfile
  - run: node scripts/check-i18n.mjs
```

- [ ] **Step 3：本地 dry-run 验证 workflow 文件能跑**

Run:

```bash
node scripts/check-i18n.mjs
```

Expected: `✓ i18n check passed: 2 locales, key 集合一致`

（如 Task 14 Step 6 跑过，这里不会重跑。）

- [ ] **Step 4：commit**

```bash
git add .github/workflows/i18n-check.yml
git commit -m "ci(v5): 新增 i18n-check workflow"
```

---

### Task 16：版本 bump + 全工作区验证

**Files:**

- Modify: `client/package.json`（version 0.0.3 → 0.0.4）
- Modify: `client/src-tauri/tauri.conf.json`（version 0.0.3 → 0.0.4）
- Modify: `versions/compat-matrix.json`（client version 0.0.3 → 0.0.4）

- [ ] **Step 1：读 client/package.json 当前 version**

Run:

```bash
grep '"version"' client/package.json
```

Expected: `"version": "0.0.3"`

- [ ] **Step 2：改 client/package.json version**

打开 `client/package.json`，把 `"version": "0.0.3"` 改为 `"version": "0.0.4"`。

- [ ] **Step 3：读 tauri.conf.json 当前 version**

Run:

```bash
grep '"version"' client/src-tauri/tauri.conf.json
```

Expected: `"version": "0.0.3"`

- [ ] **Step 4：改 tauri.conf.json version**

打开 `client/src-tauri/tauri.conf.json`，把 `"version": "0.0.3"` 改为 `"0.0.4"`。

- [ ] **Step 5：读 compat-matrix.json**

Run:

```bash
cat versions/compat-matrix.json
```

当前应为：

```json
{
  "schema": 1,
  "components": {
    "client": { "version": "0.0.3" },
    "gateway": { "version": "0.0.3" },
    "core": { "version": "0.0.3" }
  },
  "compat": {
    "client": { "gateway": ">=0.0.3 <0.1.0" },
    "gateway": { "core": ">=0.0.3 <0.1.0" },
    "core": {}
  }
}
```

- [ ] **Step 6：改 compat-matrix.json：client 0.0.3 → 0.0.4**

把 `components.client.version` 从 `"0.0.3"` 改为 `"0.0.4"`。**其他字段不动**（gateway/core 仍 0.0.3，compat range 不变）。

最终：

```json
{
  "schema": 1,
  "components": {
    "client": { "version": "0.0.4" },
    "gateway": { "version": "0.0.3" },
    "core": { "version": "0.0.3" }
  },
  "compat": {
    "client": { "gateway": ">=0.0.3 <0.1.0" },
    "gateway": { "core": ">=0.0.3 <0.1.0" },
    "core": {}
  }
}
```

- [ ] **Step 7：跑 compat-matrix-check 与 i18n-check（确保 schema 不破坏）**

Run:

```bash
node scripts/check-compat-matrix.mjs && node scripts/check-i18n.mjs
```

Expected: 两个脚本都通过

- [ ] **Step 8：跑全工作区 typecheck + test + lint 兜底**

Run:

```bash
pnpm -r typecheck && pnpm -r test && pnpm -r lint
```

Expected: 三个子项目全过（typecheck Done + 全测试 PASS + lint 0 warning）

- [ ] **Step 9：检查 git status 确认无遗漏**

Run:

```bash
git status
```

Expected: 仅 `versions/v5.md`（已 commit）+ 4 个本 task 改动的文件（client/package.json、client/src-tauri/tauri.conf.json、versions/compat-matrix.json、可能 client/test/App.test.tsx 如 Task 13 改了）需要 commit

- [ ] **Step 10：commit 版本 bump**

```bash
git add client/package.json client/src-tauri/tauri.conf.json versions/compat-matrix.json
git commit -m "feat(v5): client 0.0.3 → 0.0.4（仅 client bump，gateway/core 不动）"
```

如 Task 13 改了 App.test.tsx，一起加：

```bash
git add client/test/App.test.tsx
git commit -m "test(v5): App.test.tsx 中文断言改 i18n.t(key)"
```

- [ ] **Step 11：git log 验证 commit 历史**

Run:

```bash
git log --oneline -20
```

Expected: 顶部应是 v5 实施 commit（docs → build → feat × N → ci → test → feat 版本 bump），最底部是 v4 commit

- [ ] **Step 12：最终 push**

```bash
git push -u origin dev-20260609
```

Expected: 推送成功；CI（`i18n-check` + `version-bump-check` + `compat-matrix-check` + `quality`）全绿

如有 CI 红：本地 `node scripts/check-i18n.mjs` / `pnpm -r typecheck && pnpm -r test` 复现 + 修。

---

## 收尾检查清单

- [ ] **dev-20260609 分支**有 v5 实施全部 commits（docs + build + 资源 + init + setup + 5 组件 + switch + App + check-i18n + ci workflow + 版本 bump）
- [ ] **i18n-check CI job** 在 PR 中显示为绿
- [ ] **version-bump-check CI job** 接受 client 0.0.4 bump（gateway/core 不 bump）
- [ ] **compat-matrix-check CI job** 仍通过（schema 1，client 0.0.4）
- [ ] **quality job**（typecheck + lint + test）全过
- [ ] **手工 E2E**（不强制，每次 release 前冒烟）：
  - 启动 client（zh-CN 默认）→ 点 LanguageSwitcher → 切到 en → 全部 UI 文字变 en
  - 关闭 client → 重启 → 仍 en（localStorage 持久化）
  - 故意 en.json 缺一个 key → 看 UI 是否降级到 zh-CN
  - 隐私模式启动浏览器 localStorage 不可用 → 仍能正常启动
- [ ] **准备好 PR**（push 后走 squash merge）

---

## 附录：与 spec 的对应

| spec 节                         | 涉及 Task                                           |
| ------------------------------- | --------------------------------------------------- |
| §5.1 架构总览                   | Task 2 / 3 / 4 / 5 / 11                             |
| §5.2 i18n 初始化                | Task 4                                              |
| §5.3 资源 key 命名空间          | Task 3                                              |
| §5.4 组件重构范围               | Task 6 / 7 / 8 / 9 / 10 / 12 / 13                   |
| §5.5 LanguageSwitcher           | Task 11                                             |
| §5.6 错误码本地化               | Task 9（friendlyApiError 重构）                     |
| §5.7 Settings STATUS_LABEL 重构 | Task 8                                              |
| §5.8 持久化与启动降级           | Task 4 / 5                                          |
| §5.9 测试策略                   | Task 4 / 5 / 6 / 7 / 8 / 9 / 10 / 11 / 13 / 14      |
| §5.10 CI 校验                   | Task 14 / 15                                        |
| §5.11 错误处理                  | Task 4（catch）+ Task 11（localStorage 静默 catch） |
| §5.12 版本与兼容                | Task 16                                             |
