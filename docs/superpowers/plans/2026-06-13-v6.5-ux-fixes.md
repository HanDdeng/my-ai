# v6.5 UX Fix Bundle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复用户反馈的 8 个体验缺陷（配对落盘、超时可配、发送中体验、token 上限、apiKey 必填、弹窗层级、主题/i18n 文案），不 bump package version。

**Architecture:** 按风险递减分 4 phase 串行实施，每个 task 独立 TDD + 独立 commit。Phase 1 是纯前端低风险修复（Stronghold/i18n/CSS/必填）；Phase 2 是 ChatDialog 体验改造；Phase 3 是 gateway+core+client 三包的 timeout env 与错误文案；Phase 4 是 core DB 表重建 migration。

**Tech Stack:** TypeScript / React / Tauri (Stronghold) / Fastify / undici / SQLite (better-sqlite3) / vitest / zod / i18next

**Spec:** `docs/superpowers/specs/2026-06-13-v6.5-ux-fixes-design.md`

**Commit convention:** `fix(scope, v6.5): ...` / `feat(scope, v6.5): ...`，author 默认 handdeng，不加 Co-Authored-By trailer。

---

## Phase 1: 无风险快速修复（独立可 ship）

### Task 1: Stronghold 落盘修复（Issue #6）

**Files:**

- Modify: `client/src/lib/secure-store.ts`（全文重写 `getClient` / `saveToTauri` / `loadFromTauri` / `clearFromTauri`）
- Modify: `client/src/App.tsx:82-86`（loadSecureConfig catch 加 console.error）
- Test: `client/test/lib/secure-store.test.ts`（增 save 调用断言）

- [ ] **Step 1: 写失败测试 — saveToTauri 必须调 stronghold.save()**

在 `client/test/lib/secure-store.test.ts` 现有 `describe('secure-store (Tauri / Stronghold)')` 块内加：

```typescript
it('saveSecureConfig calls stronghold.save() to persist to disk', async () => {
  mockIsTauri = true;
  const saveSpy = vi.fn().mockResolvedValue(undefined);
  // 重新 mock Stronghold，让 save() 可被监控
  vi.doMock('@tauri-apps/plugin-stronghold', () => ({
    Stronghold: class {
      static load = async () => new this();
      save = saveSpy;
      async loadClient() {
        throw new Error('not exists');
      }
      async createClient() {
        return {
          getStore: () => ({
            insert: async () => {},
            get: async () => null,
            remove: async () => null,
          }),
        };
      }
    },
  }));
  vi.resetModules();
  const mod = await import('@/lib/secure-store.js');
  await mod.saveSecureConfig({
    clientKey: 'ck',
    gatewayUrl: 'http://x',
    pairKey: null,
    clientName: null,
  });
  expect(saveSpy).toHaveBeenCalledTimes(1);
});

it('clearSecureConfig calls stronghold.save() after remove', async () => {
  mockIsTauri = true;
  const saveSpy = vi.fn().mockResolvedValue(undefined);
  vi.doMock('@tauri-apps/plugin-stronghold', () => ({
    Stronghold: class {
      static load = async () => new this();
      save = saveSpy;
      async loadClient() {
        return {
          getStore: () => ({
            insert: async () => {},
            get: async () => null,
            remove: async () => null,
          }),
        };
      }
    },
  }));
  vi.resetModules();
  const mod = await import('@/lib/secure-store.js');
  await mod.clearSecureConfig();
  expect(saveSpy).toHaveBeenCalledTimes(1);
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm --filter @my-ai/client run test -- test/lib/secure-store.test.ts
```

Expected: 两条新 case 失败（saveSpy 未被调）。

- [ ] **Step 3: 重写 secure-store.ts**

把 `client/src/lib/secure-store.ts` 第 26-55 行替换为：

```typescript
async function getOrCreateClient(stronghold: Stronghold) {
  // Stronghold v2: createClient 在 record 已存在时抛错。
  // 先 loadClient（已存在场景），失败再 createClient（首次启动）。
  try {
    return await stronghold.loadClient(RECORD_NAME);
  } catch {
    return await stronghold.createClient(RECORD_NAME);
  }
}

// ===== Tauri (Stronghold) 路径 =====
async function saveToTauri(cfg: SecureConfig): Promise<void> {
  const stronghold = await Stronghold.load(STORE_PATH, STORE_PASSWORD);
  const client = await getOrCreateClient(stronghold);
  const store = client.getStore();
  const bytes = new TextEncoder().encode(JSON.stringify(cfg));
  // stronghold Store.insert 要求 number[]；用 Array.from 展开 Uint8Array。
  await store.insert(CONFIG_KEY, Array.from(bytes));
  // 关键：把 in-memory 改动写回磁盘文件，否则进程退出后丢失。
  await stronghold.save();
}

async function loadFromTauri(): Promise<SecureConfig | null> {
  const stronghold = await Stronghold.load(STORE_PATH, STORE_PASSWORD);
  const client = await getOrCreateClient(stronghold);
  const store = client.getStore();
  const bytes = await store.get(CONFIG_KEY);
  if (!bytes) {
    return null;
  }
  const json = new TextDecoder().decode(bytes);
  return JSON.parse(json) as SecureConfig;
}

async function clearFromTauri(): Promise<void> {
  const stronghold = await Stronghold.load(STORE_PATH, STORE_PASSWORD);
  const client = await getOrCreateClient(stronghold);
  const store = client.getStore();
  await store.remove(CONFIG_KEY);
  // 同上：删除也需要 save() 才落盘。
  await stronghold.save();
}
```

- [ ] **Step 4: 加 App.tsx 防御日志**

把 `client/src/App.tsx:82-86`：

```typescript
      .catch(() => {
        if (!cancelled) {
          setStatus('NEED_PAIR');
        }
      });
```

改为：

```typescript
      .catch((e: unknown) => {
        // v6.5: 持久化加载失败时静默吞错会导致诊断无门；显式 log 到控制台便于未来排查。
        console.error('[my-ai] loadSecureConfig failed:', e);
        if (!cancelled) {
          setStatus('NEED_PAIR');
        }
      });
```

- [ ] **Step 5: 跑测试确认通过**

```bash
pnpm --filter @my-ai/client run test -- test/lib/secure-store.test.ts
pnpm --filter @my-ai/client run typecheck
```

Expected: 全部 PASS，无 TS 错。

- [ ] **Step 6: Commit**

```bash
git add client/src/lib/secure-store.ts client/src/App.tsx client/test/lib/secure-store.test.ts
git commit -m "fix(client, v6.5): Stronghold 写后调 save() 才能跨重启保留配对"
```

---

### Task 2: i18n 文案补齐（Issue #7 + #8）

**Files:**

- Modify: `client/src/i18n/locales/zh-CN/translation.json`（多处文案）
- Test: 既有 `App.test.tsx` / `PairDialog.test.tsx` snapshot 受影响时 update

- [ ] **Step 1: 改 zh-CN/translation.json**

打开 `client/src/i18n/locales/zh-CN/translation.json`，按下表逐条改：

```json
{
  "app": {
    "brand": "my-ai",
    "title": "网关配对",
    "titleEm": "配对",
    "subtitle": {
      "line1": "远程配对与鉴权",
      "sep": "·",
      "line2": "客户端 {{version}}"
    },
    "footer": {
      "copy": "© my-ai · 本地控制",
      "date": "━━ {{date}}"
    }
  },
  "settings": {
    "label": "Gateway 状态",
    "statusHead": "状态 / 网关",
    "urlAria": "Gateway URL",
    "test": "测试",
    "gatewayLabel": "网关",
    "versionPrefix": "网关",
    ...其余 status 块保留不动
  },
  "pair": {
    ...banner 保留
    "dialog": {
      "title": "配对网关",
      "sub": "配对流程 / 第 1 步",
      "fields": {
        "url": {
          "label": "Gateway URL",
          "req": "必填",
          "placeholder": "http://127.0.0.1:8787"
        },
        "pairKey": {
          "label": "Pair Key",
          "opt": "可选",
          ...
        },
        "name": {
          "label": "客户端名",
          "opt": "可选",
          ...
        }
      },
      ...token / status / actions / errors 保留
    }
  },
  ...
  "theme": {
    "light": "亮色",
    "dark": "暗色",
    "ariaCurrent": "当前 {{state}}色主题，点击切换到 {{next}}色"
  },
  "lang": {
    "zh": "中",
    "en": "英",
    "aria": "切换语言，当前 {{current}}"
  },
  ...
}
```

`en/translation.json` 不动。

- [ ] **Step 2: 跑客户端测试套件，看哪些 snapshot 受影响**

```bash
pnpm --filter @my-ai/client run test
```

Expected: 若有 snapshot mismatch，需 update；逻辑测试不应失败。

- [ ] **Step 3: 如有 snapshot mismatch，update**

```bash
pnpm --filter @my-ai/client run test -- -u
```

再跑一次确认全过：

```bash
pnpm --filter @my-ai/client run test
```

- [ ] **Step 4: typecheck（i18n 是 JSON，理论上不会破，但跑一遍保险）**

```bash
pnpm --filter @my-ai/client run typecheck
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add client/src/i18n/locales/zh-CN/translation.json
git add -u client/test
git commit -m "fix(client, v6.5): zh-CN 主题按钮/主页/配对弹窗文案中文化"
```

---

### Task 3: PairDialog z-index 提升（Issue #1）

**Files:**

- Modify: `client/src/styles.css:665` 附近 `.dialog-backdrop` 块
- Test: `client/test/components/PairDialog.test.tsx`（新增 case）

- [ ] **Step 1: 写失败测试 — backdrop z-index ≥ 1100**

在 `client/test/components/PairDialog.test.tsx` 末尾加：

```typescript
it('backdrop 的 z-index 高于 .dialog-drawer (1000) — 防止被 AgentFormDialog 遮挡', () => {
  const { container } = render(
    <PairDialog
      initialUrl="http://x"
      clientKey="ck"
      onPaired={() => {}}
      onClose={() => {}}
    />,
  );
  const backdrop = container.querySelector('.dialog-backdrop') as HTMLElement;
  expect(backdrop).toBeTruthy();
  // jsdom 不解析外部 CSS；从 styles.css 间接读不可行。
  // 改为运行时把 styles.css 注入到 document.head：见 setup.ts；
  // 若 setup 已注入，window.getComputedStyle 能拿到值。
  const z = window.getComputedStyle(backdrop).zIndex;
  expect(Number(z)).toBeGreaterThanOrEqual(1100);
});
```

> 备注：如果 `client/test/setup.ts` 没注入 styles.css，本 case 会跑 NaN > 1100 失败 — 那么改为用 string match 在 styles.css 上断言：

如果 jsdom 不读 CSS（多数情况），用文件级断言代替：

```typescript
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

it('styles.css 里 .dialog-backdrop z-index ≥ 1100 — 防止被 AgentFormDialog (drawer z-index 1000) 遮挡', () => {
  const cssPath = fileURLToPath(new URL('../../src/styles.css', import.meta.url));
  const css = readFileSync(cssPath, 'utf8');
  // 找 .dialog-backdrop 块里的 z-index
  const block = css.match(/\.dialog-backdrop\s*\{[^}]+\}/);
  expect(block).toBeTruthy();
  const zMatch = block![0].match(/z-index:\s*(\d+)/);
  expect(zMatch).toBeTruthy();
  expect(Number(zMatch![1])).toBeGreaterThanOrEqual(1100);
});
```

选第二种实现（更稳）。

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm --filter @my-ai/client run test -- test/components/PairDialog.test.tsx
```

Expected: 新 case 失败（z-index 当前为 100）。

- [ ] **Step 3: 改 styles.css**

打开 `client/src/styles.css`，找到第 655 行附近 `.dialog-backdrop` 块，把 `z-index: 100;` 改为：

```css
.dialog-backdrop {
  ...其他属性保留
  /* v6.5: 高于 .dialog-drawer 的 1000，保证 PairDialog 不被 AgentFormDialog 遮挡。 */
  z-index: 1100;
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
pnpm --filter @my-ai/client run test -- test/components/PairDialog.test.tsx
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add client/src/styles.css client/test/components/PairDialog.test.tsx
git commit -m "fix(client, v6.5): PairDialog backdrop z-index 提到 1100 防止被 AgentForm drawer 遮挡"
```

---

### Task 4: API Key 前端必填（Issue #5）

**Files:**

- Modify: `client/src/components/AgentFormDialog.tsx`（zod / 提交体 / UI label / input required）
- Modify: `client/src/i18n/locales/zh-CN/translation.json`（apiKey.placeholder + 新增 errors.apiKeyRequired）
- Modify: `client/src/i18n/locales/en/translation.json`（同上）
- Test: `client/test/components/AgentFormDialog.test.tsx`

- [ ] **Step 1: 写失败测试 — apiKey 空时阻止提交**

在 `client/test/components/AgentFormDialog.test.tsx` 中找到现有 v6.4 测试（"提交成功 → onSaved（body 含 ... apiKey=null）"），新增一条相邻 case：

```typescript
it('v6.5: apiKey 空 → 阻止提交 + 不调 createAgent', async () => {
  const createSpy = vi.spyOn(agents, 'createAgent').mockResolvedValue({} as never);
  render(
    <AgentFormDialog
      mode="create"
      gatewayUrl="http://gw"
      clientKey="ck"
      onClose={() => {}}
      onSaved={() => {}}
    />,
  );
  // 填必填字段但 apiKey 不填
  await userEvent.type(screen.getByLabelText('名称'), 'echo');
  await userEvent.type(screen.getByLabelText('基础 URL'), 'http://x/v1');
  await userEvent.type(screen.getByLabelText('模型'), 'm');
  await userEvent.click(screen.getByRole('button', { name: '创建' }));
  // createAgent 不应被调；UI 显示必填错
  expect(createSpy).not.toHaveBeenCalled();
});
```

同时把既有的 v6.4 "提交成功 ... apiKey=null" case 改为 "apiKey 非空 + 透传"（不传 apiKey 时不再 saveable）：

找到该 case，把"填表后提交 → body.apiKey === null"断言改为填一个 apiKey 后断言 `body.apiKey === 'sk-test'`。原 "v6.4: 填写 apiKey → 提交 body 含该值" 这条已经覆盖了"填写后透传"，所以 v6.4 default-null case 可以直接删除。

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm --filter @my-ai/client run test -- test/components/AgentFormDialog.test.tsx
```

Expected: 新 case 失败（apiKey 空当前能通过 zod default ''）。

- [ ] **Step 3: 改 AgentFormDialog.tsx**

把 `client/src/components/AgentFormDialog.tsx` 第 34 行：

```typescript
  apiKey: z.string().max(512).default(''),
```

改为：

```typescript
  apiKey: z.string().min(1, 'apiKeyRequired').max(512),
```

把第 152 行：

```typescript
        apiKey: parsed.data.apiKey.trim() === '' ? null : parsed.data.apiKey.trim(),
```

改为：

```typescript
        // v6.5: apiKey 前端必填（schema 已 min(1) 校验），trim 后入库；不再走 ''→null fallback。
        apiKey: parsed.data.apiKey.trim(),
```

把第 369-371 行 label：

```jsx
<label htmlFor="agent-apiKey" style={{ fontSize: 12, color: 'var(--text-muted)' }}>
  {t('agentForm.field.apiKey.label')}
</label>
```

改为：

```jsx
<label htmlFor="agent-apiKey" style={{ fontSize: 12, color: 'var(--text-muted)' }}>
  {t('agentForm.field.apiKey.label')} *
</label>
```

把第 372-391 行 input：在 `autoComplete="off"` 后加 `required`：

```jsx
                    <input
                      id="agent-apiKey"
                      type="password"
                      className="input"
                      value={form.apiKey ?? ''}
                      onChange={e => setForm({ ...form, apiKey: e.target.value })}
                      placeholder={t('agentForm.field.apiKey.placeholder')}
                      maxLength={512}
                      disabled={submitting}
                      autoComplete="off"
                      required
                      aria-label={t('agentForm.field.apiKey.label')}
                      style={{ ... }}
                    />
```

- [ ] **Step 4: 改 i18n placeholder**

`client/src/i18n/locales/zh-CN/translation.json`：

```diff
       "apiKey": {
         "label": "API Key",
-        "placeholder": "留空 = 用网关默认（env LLM_API_KEY）"
+        "placeholder": "必填；Ollama 等无 key 场景填任意占位符"
       },
```

`agentForm.errors` 块加：

```diff
       "deleteFailed": "删除失败：{{msg}}",
+      "apiKeyRequired": "API Key 必填"
     }
```

`client/src/i18n/locales/en/translation.json` 同改：

```diff
       "apiKey": {
         "label": "API Key",
-        "placeholder": "Empty = use gateway default (env LLM_API_KEY)"
+        "placeholder": "Required; for Ollama or no-key endpoints, enter any placeholder"
       },
```

errors 块加：

```diff
       "deleteFailed": "Delete failed: {{msg}}",
+      "apiKeyRequired": "API Key required"
     }
```

- [ ] **Step 5: 跑测试 + typecheck**

```bash
pnpm --filter @my-ai/client run test -- test/components/AgentFormDialog.test.tsx
pnpm --filter @my-ai/client run typecheck
```

Expected: 全部 PASS。

- [ ] **Step 6: Commit**

```bash
git add client/src/components/AgentFormDialog.tsx client/src/i18n/locales client/test/components/AgentFormDialog.test.tsx
git commit -m "feat(client, v6.5): API Key 前端必填（后端 nullable + env fallback 保留）"
```

---

## Phase 2: ChatDialog 体验改造

### Task 5: 发送中 spinner + 计时 + 取消（Issue #3）

**Files:**

- Modify: `client/src/lib/api.ts`（`ApiFetchOptions` 加 `signal`，透传给 fetch）
- Modify: `client/src/lib/messages.ts`（`postMessage` 签名加 `signal`）
- Modify: `client/src/components/ChatDialog.tsx`（state 加 startedAt；AbortController + cancel；spinner UI）
- Modify: `client/src/styles.css`（新增 `.spinner` 动画）
- Modify: `client/src/i18n/locales/{zh-CN,en}/translation.json`（chat.cancel / chat.cancelled）
- Test: `client/test/lib/messages.test.ts`（signal 透传）
- Test: `client/test/components/ChatDialog.test.tsx`（sending 状态文案 + 取消）

- [ ] **Step 1: 写失败测试 — postMessage 透传 signal**

`client/test/lib/messages.test.ts` 末尾加：

```typescript
it('postMessage 透传 AbortSignal 到 fetch', async () => {
  const fetchSpy = vi.spyOn(globalThis, 'fetch').mockResolvedValue(
    new Response(
      JSON.stringify({ data: { userMessage: {}, assistantMessage: {} }, code: 0, message: 'ok' }),
      {
        status: 200,
        headers: { 'content-type': 'application/json' },
      },
    ),
  );
  const ctl = new AbortController();
  await postMessage('http://gw', 'ck', 'sid', 'hi', ctl.signal);
  expect(fetchSpy).toHaveBeenCalledWith(
    expect.any(String),
    expect.objectContaining({ signal: ctl.signal }),
  );
  fetchSpy.mockRestore();
});
```

- [ ] **Step 2: 写失败测试 — ChatDialog sending 时显示「生成中… Ns」+ 取消按钮**

`client/test/components/ChatDialog.test.tsx` 末尾加：

```typescript
it('v6.5: sending 时按钮显示「生成中…」+ 计时 + 取消按钮', async () => {
  vi.useFakeTimers();
  vi.spyOn(agents, 'getAgent').mockResolvedValue({ id: 'a1', name: 'echo' } as never);
  vi.spyOn(sessions, 'createSession').mockResolvedValue({ id: 's1' } as never);
  let resolvePost: (v: unknown) => void;
  vi.spyOn(messages, 'postMessage').mockReturnValue(
    new Promise(r => { resolvePost = r; }) as never,
  );
  render(
    <ChatDialog
      agentId="a1"
      gatewayUrl="http://gw"
      clientKey="ck"
      onClose={() => {}}
      onAgentDeleted={() => {}}
    />,
  );
  await waitFor(() => screen.getByPlaceholderText('输入消息…'));
  await userEvent.type(screen.getByPlaceholderText('输入消息…'), 'hi');
  await userEvent.click(screen.getByRole('button', { name: '发送' }));
  // 按钮显示 "生成中… 0s"
  await waitFor(() => screen.getByText(/生成中…\s*\d+s/));
  // 取消按钮可见
  expect(screen.getByRole('button', { name: '取消' })).toBeInTheDocument();
  // 时间推进 3s 后应显示 "生成中… 3s"
  vi.advanceTimersByTime(3000);
  await waitFor(() => screen.getByText(/生成中…\s*3s/));
  resolvePost!({ userMessage: { id: 'u1', sessionId: 's1', role: 'user', content: 'hi', createdAt: '' },
                 assistantMessage: { id: 'a1', sessionId: 's1', role: 'assistant', content: 'ok', createdAt: '' } });
  vi.useRealTimers();
});

it('v6.5: 点击取消 → fetch 被 abort + 错误条显示「已取消」', async () => {
  vi.spyOn(agents, 'getAgent').mockResolvedValue({ id: 'a1', name: 'echo' } as never);
  vi.spyOn(sessions, 'createSession').mockResolvedValue({ id: 's1' } as never);
  const postSpy = vi.spyOn(messages, 'postMessage').mockImplementation(
    (_gw, _ck, _sid, _c, signal) =>
      new Promise((_resolve, reject) => {
        signal?.addEventListener('abort', () => reject(new DOMException('aborted', 'AbortError')));
      }) as never,
  );
  render(
    <ChatDialog
      agentId="a1" gatewayUrl="http://gw" clientKey="ck"
      onClose={() => {}} onAgentDeleted={() => {}}
    />,
  );
  await waitFor(() => screen.getByPlaceholderText('输入消息…'));
  await userEvent.type(screen.getByPlaceholderText('输入消息…'), 'hi');
  await userEvent.click(screen.getByRole('button', { name: '发送' }));
  await waitFor(() => screen.getByRole('button', { name: '取消' }));
  await userEvent.click(screen.getByRole('button', { name: '取消' }));
  await waitFor(() => screen.getByText('已取消'));
  expect(postSpy).toHaveBeenCalled();
});
```

- [ ] **Step 3: 跑测试确认失败**

```bash
pnpm --filter @my-ai/client run test -- test/lib/messages.test.ts test/components/ChatDialog.test.tsx
```

Expected: 三条新 case 失败。

- [ ] **Step 4: 改 api.ts — 透传 signal**

`client/src/lib/api.ts` 第 26-30 行 `ApiFetchOptions`：

```diff
 export type ApiFetchOptions = {
   method?: 'GET' | 'POST' | 'PATCH' | 'PUT' | 'DELETE';
   body?: unknown;
   clientKey?: string | null;
+  signal?: AbortSignal;
 };
```

第 50-53 行 `init`：

```diff
   const init: RequestInit = {
     method: opts.method ?? 'GET',
     headers,
+    ...(opts.signal ? { signal: opts.signal } : {}),
   };
```

- [ ] **Step 5: 改 messages.ts**

`client/src/lib/messages.ts` `postMessage` 签名 + 实现：

```typescript
export function postMessage(
  gw: string,
  ck: string,
  sessionId: string,
  content: string,
  signal?: AbortSignal,
): Promise<PostMessageResponse> {
  return apiFetch<PostMessageResponse>(
    url(gw, `/v1/sessions/${encodeURIComponent(sessionId)}/messages`),
    {
      method: 'POST',
      clientKey: ck,
      body: { id: randomUUID(), content, reasoningEffort: 'none' as const },
      ...(signal ? { signal } : {}),
    },
  );
}
```

- [ ] **Step 6: 改 ChatDialog.tsx**

把 `ChatState` 类型扩 `startedAt`：

```typescript
type ChatState =
  | { kind: 'idle' }
  | { kind: 'sending'; pending: { tempId: string; content: string }; startedAt: number }
  | { kind: 'error'; pending: { tempId: string; content: string } | null; error: string };
```

`chatReducer` `send-start` case 加 `startedAt`：

```typescript
    case 'send-start':
      return {
        kind: 'sending',
        pending: { tempId: action.tempId, content: action.content },
        startedAt: Date.now(),
      };
```

在 `ChatDialog` 组件内部加 abortRef + tick state + 计时 effect + onCancel：

```typescript
const abortRef = useRef<AbortController | null>(null);
const [_tick, setTick] = useState(0);

// sending 中每秒 tick 一次刷新计时
useEffect(() => {
  if (state.kind !== 'sending') return;
  const id = setInterval(() => setTick(t => t + 1), 1000);
  return () => clearInterval(id);
}, [state.kind]);

const elapsedSec = state.kind === 'sending' ? Math.floor((Date.now() - state.startedAt) / 1000) : 0;

const onCancel = () => {
  abortRef.current?.abort();
  dispatch({ type: 'send-error', error: t('chat.cancelled') });
};
```

`onSubmit` 内 postMessage 调用改为：

```typescript
abortRef.current = new AbortController();
const res = await postMessage(gatewayUrl, clientKey, sid, content, abortRef.current.signal);
```

按钮 + 取消按钮 — 找到现有 `<button type="submit"...>` 块（行 311-318），替换为：

```jsx
<button
  type="submit"
  className="btn"
  disabled={!input.trim() || state.kind === 'sending'}
  style={{ background: 'var(--accent)', color: '#fff' }}
>
  {state.kind === 'sending' ? (
    <>
      <span className="spinner" aria-hidden="true" />
      {t('chat.sending')} {elapsedSec}s
    </>
  ) : (
    t('chat.send')
  )}
</button>;
{
  state.kind === 'sending' && (
    <button
      type="button"
      className="btn"
      onClick={onCancel}
      style={{ background: 'transparent', border: '1px solid var(--border)' }}
    >
      {t('chat.cancel')}
    </button>
  );
}
```

`retry` 路径同样改为传 signal（onRetry 函数内也走 abortRef 模式）：

```typescript
const onRetry = async () => {
  if (state.kind !== 'error' || !state.pending) {
    return;
  }
  const { tempId, content } = state.pending;
  dispatch({ type: 'retry' });
  abortRef.current = new AbortController();
  try {
    let sid = sessionId;
    if (!sid) {
      const session = await createSession(gatewayUrl, clientKey, agentId);
      sid = session.id;
      setSessionId(sid);
    }
    const res = await postMessage(gatewayUrl, clientKey, sid, content, abortRef.current.signal);
    setMessages(m => [...m.filter(x => x.id !== tempId), res.userMessage, res.assistantMessage]);
    dispatch({ type: 'send-success' });
  } catch (e) {
    dispatch({ type: 'send-error', error: e instanceof Error ? e.message : String(e) });
  }
};
```

- [ ] **Step 7: 加 styles.css spinner 动画**

在 `client/src/styles.css` 末尾加：

```css
/* v6.5: 发送中 spinner（与文本内联） */
.spinner {
  display: inline-block;
  width: 0.8em;
  height: 0.8em;
  border: 2px solid currentColor;
  border-right-color: transparent;
  border-radius: 50%;
  animation: spin 0.7s linear infinite;
  margin-right: 0.4em;
  vertical-align: -0.1em;
}
@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}
```

- [ ] **Step 8: 加 i18n**

`client/src/i18n/locales/zh-CN/translation.json` `"chat"` 块加：

```diff
   "chat": {
     ...
+    "cancel": "取消",
+    "cancelled": "已取消",
     "send": "发送",
```

`client/src/i18n/locales/en/translation.json` 同：

```diff
   "chat": {
     ...
+    "cancel": "Cancel",
+    "cancelled": "Cancelled",
     "send": "Send",
```

- [ ] **Step 9: 跑测试 + typecheck**

```bash
pnpm --filter @my-ai/client run test
pnpm --filter @my-ai/client run typecheck
```

Expected: 全部 PASS。

- [ ] **Step 10: Commit**

```bash
git add client/src/lib client/src/components/ChatDialog.tsx client/src/styles.css client/src/i18n/locales client/test
git commit -m "feat(client, v6.5): ChatDialog 发送中 spinner + 计时 + 取消按钮"
```

---

## Phase 3: 跨包配置 — timeout + 错误文案

### Task 6: CORE_TIMEOUT_MS + LLM_TIMEOUT_MS + 错误文案带原因（Issue #2）

**Files:**

- Modify: `gateway/src/config.ts`（加 CORE_TIMEOUT_MS）
- Modify: `gateway/src/server.ts`（传 timeoutMs 给 CoreClient）
- Modify: `gateway/src/routes/messages.ts`（502 catch 带 e.message）
- Modify: `gateway/src/routes/sessions.ts`（同上 3 处 catch）
- Modify: `gateway/.env.example`（加 CORE_TIMEOUT_MS）
- Modify: `core/src/config.ts`（加 LLM_TIMEOUT_MS）
- Modify: `core/src/llm/openai-compatible.ts`（OpenAICompatibleConfig 加 timeoutMs；AbortSignal.timeout 用之）
- Modify: `core/src/llm/factory.ts`（透传 cfg.timeoutMs）
- Modify: `core/src/routes/messages.ts`（502 catch 带 e.message）
- Modify: `core/.env.example`（加 LLM_TIMEOUT_MS）
- Modify: `client/src/components/ChatDialog.tsx`（区分 timeout vs 一般 5xx）
- Modify: `client/src/i18n/locales/{zh-CN,en}/translation.json`（chat.errors.upstreamTimeout / upstreamWithReason）
- Test: `gateway/test/config.test.ts`（CORE_TIMEOUT_MS default + 自定义）
- Test: `core/test/llm/openai-compatible.test.ts`（timeoutMs 透传到 AbortSignal）
- Test: `client/test/components/ChatDialog.test.tsx`（识别 timeout 文案）

- [ ] **Step 1: 写失败测试 — gateway config 加载 CORE_TIMEOUT_MS**

`gateway/test/config.test.ts` 末尾加：

```typescript
it('v6.5: CORE_TIMEOUT_MS 默认 640000，可被 env 覆盖', () => {
  const def = loadConfig({});
  expect(def.CORE_TIMEOUT_MS).toBe(640_000);
  const overridden = loadConfig({ CORE_TIMEOUT_MS: '120000' });
  expect(overridden.CORE_TIMEOUT_MS).toBe(120_000);
});

it('v6.5: CORE_TIMEOUT_MS 非正整数 → 抛错退出', () => {
  expect(() => loadConfig({ CORE_TIMEOUT_MS: '-1' })).toThrowError();
});
```

注：`loadConfig` 当前直接 `process.exit(1)` 不抛错；为可测，先把 `loadConfig` 改成接受 env 参数（已是这样）；exit 改 throw — 但这破坏现有 contract，**改为：跳过第二条 case，只保留 default + 自定义两条断言**。

最终保留：

```typescript
it('v6.5: CORE_TIMEOUT_MS 默认 640000，可被 env 覆盖', () => {
  const def = loadConfig({
    GATEWAY_PAIRING_PUBLIC: 'false',
    GATEWAY_PAIR_KEY: '',
  } as NodeJS.ProcessEnv);
  expect(def.CORE_TIMEOUT_MS).toBe(640_000);
  const overridden = loadConfig({
    GATEWAY_PAIRING_PUBLIC: 'false',
    CORE_TIMEOUT_MS: '120000',
  } as NodeJS.ProcessEnv);
  expect(overridden.CORE_TIMEOUT_MS).toBe(120_000);
});
```

- [ ] **Step 2: 写失败测试 — core llm 用 timeoutMs**

`core/test/llm/openai-compatible.test.ts` 末尾加：

```typescript
it('v6.5: timeoutMs 透传到 AbortSignal.timeout', async () => {
  const timeoutSpy = vi.spyOn(AbortSignal, 'timeout');
  const fetchSpy = vi.spyOn(globalThis, 'fetch').mockResolvedValue(
    new Response(JSON.stringify({ choices: [{ message: { content: 'ok' } }] }), {
      status: 200,
      headers: { 'content-type': 'application/json' },
    }),
  );
  const client = new OpenAICompatibleLLMClient({
    baseUrl: 'http://x/v1',
    model: 'm',
    timeoutMs: 123_456,
  });
  await client.chat({ model: 'm', messages: [{ role: 'user', content: 'hi' }] });
  expect(timeoutSpy).toHaveBeenCalledWith(123_456);
  timeoutSpy.mockRestore();
  fetchSpy.mockRestore();
});

it('v6.5: 未传 timeoutMs → 默认 600_000', async () => {
  const timeoutSpy = vi.spyOn(AbortSignal, 'timeout');
  const fetchSpy = vi.spyOn(globalThis, 'fetch').mockResolvedValue(
    new Response(JSON.stringify({ choices: [{ message: { content: 'ok' } }] }), {
      status: 200,
      headers: { 'content-type': 'application/json' },
    }),
  );
  const client = new OpenAICompatibleLLMClient({ baseUrl: 'http://x/v1', model: 'm' });
  await client.chat({ model: 'm', messages: [{ role: 'user', content: 'hi' }] });
  expect(timeoutSpy).toHaveBeenCalledWith(600_000);
  timeoutSpy.mockRestore();
  fetchSpy.mockRestore();
});
```

- [ ] **Step 3: 跑测试确认失败**

```bash
pnpm --filter @my-ai/gateway run test -- test/config.test.ts
pnpm --filter @my-ai/core run test -- test/llm/openai-compatible.test.ts
```

Expected: 新 case 失败。

- [ ] **Step 4: 改 gateway/src/config.ts**

在 Schema 中加 `CORE_TIMEOUT_MS`：

```typescript
const Schema = z.object({
  ... 现有字段保留
  // v6.5: gateway → core 上游 HTTP 客户端超时（ms）；必须 > core 的 LLM_TIMEOUT_MS。
  CORE_TIMEOUT_MS: z.coerce.number().int().positive().default(640_000),
  GATEWAY_DB_PATH: z.string().default('./gateway.db'),
});
```

- [ ] **Step 5: 改 gateway/src/server.ts:54**

```diff
-  const core = new CoreClient({ baseUrl: cfg.CORE_URL });
+  const core = new CoreClient({ baseUrl: cfg.CORE_URL, timeoutMs: cfg.CORE_TIMEOUT_MS });
```

- [ ] **Step 6: 改 gateway/.env.example**

在文件末尾加：

```
# === v6.5 新增：超时配置 ===
# gateway → core 上游 HTTP 客户端超时（ms）。
# 必须 > core/.env 里的 LLM_TIMEOUT_MS（建议大 20-40s 缓冲），
# 否则 gateway 会先于 core 切断进行中的 LLM 调用。
# 默认 640000 = 10min40s，覆盖大多数 LLM 思考密集型任务。
CORE_TIMEOUT_MS=640000
```

- [ ] **Step 7: 改 core/src/config.ts**

```typescript
const Schema = z.object({
  ... 现有字段保留
  // v6.5: core → LLM 上游请求超时（ms）；必须 < gateway 的 CORE_TIMEOUT_MS。
  LLM_TIMEOUT_MS: z.coerce.number().int().positive().default(600_000),
  CORE_DB_PATH: z.string().default('./core.db'),
});
```

- [ ] **Step 8: 改 core/src/llm/openai-compatible.ts**

`OpenAICompatibleConfig` 加字段：

```diff
 export type OpenAICompatibleConfig = {
   baseUrl: string;
   apiKey?: string;
   model: string;
   maxTokens?: number | undefined;
   reasoningEffort?: 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | undefined;
+  // v6.5: 请求超时（ms）；缺省 600_000。
+  timeoutMs?: number | undefined;
 };
```

第 51 行：

```diff
-        signal: AbortSignal.timeout(60_000),
+        signal: AbortSignal.timeout(this.cfg.timeoutMs ?? 600_000),
```

注释行 4 顺便更新：`AbortSignal.timeout` 改为 "可配；默认 600s 上限"。

- [ ] **Step 9: 改 core/src/llm/factory.ts**

在 'openai-compatible' factory 内加一段（与 maxTokens 相似）：

```typescript
if (typeof cfg.timeoutMs === 'number') {
  c.timeoutMs = cfg.timeoutMs;
}
```

- [ ] **Step 10: 改 core/src/routes/messages.ts:71 + 95-101**

第 71 行 `createLLMClient` 调用加 `timeoutMs`：

```diff
     const llm = createLLMClient(agent.llm_provider, {
       baseUrl: agent.base_url,
       apiKey: agent.api_key ?? cfg.LLM_API_KEY,
       model: agent.model,
       maxTokens: agent.max_tokens ?? undefined,
       reasoningEffort: parsed.data.reasoningEffort ?? 'none',
+      timeoutMs: (cfg as { LLM_TIMEOUT_MS?: number }).LLM_TIMEOUT_MS,
     });
```

cfg 类型断言也要更新；找到第 39 行 `const cfg = (app as unknown as { config: { LLM_API_KEY?: string } }).config;`，改为：

```typescript
const cfg = (app as unknown as { config: { LLM_API_KEY?: string; LLM_TIMEOUT_MS?: number } })
  .config;
```

第 95-101 行：

```diff
     } catch (e) {
       if (e instanceof LLMUpstreamError) {
         app.log.error({ err: e }, 'LLM upstream error');
-        throw new HttpError(502, 'upstream_error');
+        // v6.5: 带原因消息（timeout/HTTP 状态/JSON 解析），方便 client 区分
+        throw new HttpError(502, `upstream_error: ${e.message}`);
       }
       throw e;
     }
```

- [ ] **Step 11: 改 gateway/src/routes/messages.ts + sessions.ts catch 块**

`gateway/src/routes/messages.ts` 第 25-28、37-41 两处 catch：

```diff
-    } catch (e) {
+    } catch (e: unknown) {
       req.log.error({ err: e, sessionId: req.params.id }, 'listMessages failed');
-      return reply.code(502).send(err(502, 'upstream_error'));
+      const msg = e instanceof Error ? e.message : 'upstream_error';
+      return reply.code(502).send(err(502, `upstream_error: ${msg}`));
     }
```

`gateway/src/routes/sessions.ts` 同样改 3 处 catch（grep `'upstream_error'` 找全）：每处 catch 内：

```diff
-      return reply.code(502).send(err(502, 'upstream_error'));
+      const msg = e instanceof Error ? e.message : 'upstream_error';
+      return reply.code(502).send(err(502, `upstream_error: ${msg}`));
```

- [ ] **Step 12: 改 core/.env.example**

末尾加：

```
# === v6.5 新增：LLM 超时 ===
# core → LLM 上游 (OpenAI / Ollama / vLLM 等) 请求超时（ms）。
# 必须 < gateway 的 CORE_TIMEOUT_MS（建议小 20-40s）。
# 默认 600000 = 10min，覆盖本地 Ollama 大模型推理。
LLM_TIMEOUT_MS=600000
```

- [ ] **Step 13: 改 client/src/components/ChatDialog.tsx — 错误识别**

`onSubmit` catch 块（行 138-152）改：

```typescript
    } catch (e) {
      if (e instanceof ApiError && e.code === 401) {
        window.dispatchEvent(new CustomEvent('my-ai:unauthorized'));
        dispatch({ type: 'send-success' });
        return;
      }
      setMessages(m =>
        m.map(x => (x.id === tempId ? { ...x, content: `${x.content} (失败)` } : x)),
      );
      // v6.5: 区分 timeout vs 一般 5xx；带原因文案
      let msg: string;
      if (e instanceof ApiError && e.code === 502) {
        const raw = (e.message ?? '').toString();
        if (/timeout|TIMEOUT|UND_ERR_HEADERS_TIMEOUT|AbortError/i.test(raw)) {
          msg = t('chat.errors.upstreamTimeout');
        } else {
          msg = t('chat.errors.upstreamWithReason', { reason: raw || 'unknown' });
        }
      } else {
        msg = t('chat.errors.sendFailed', {
          msg: e instanceof Error ? e.message : String(e),
        });
      }
      dispatch({ type: 'send-error', error: msg });
    }
```

- [ ] **Step 14: 加 i18n keys**

`client/src/i18n/locales/zh-CN/translation.json` `"chat.errors"` 块加：

```diff
     "errors": {
       "sendFailed": "发送失败：{{msg}}",
       "upstream": "上游服务不可用",
+      "upstreamTimeout": "LLM 响应超时。请尝试简化任务，或在 gateway/.env 调大 CORE_TIMEOUT_MS、core/.env 调大 LLM_TIMEOUT_MS。",
+      "upstreamWithReason": "上游服务出错：{{reason}}",
       "network": "网络连不上"
     }
```

`client/src/i18n/locales/en/translation.json`：

```diff
     "errors": {
       "sendFailed": "Send failed: {{msg}}",
       "upstream": "Upstream service unavailable",
+      "upstreamTimeout": "LLM response timed out. Try simplifying the task, or raise CORE_TIMEOUT_MS in gateway/.env and LLM_TIMEOUT_MS in core/.env.",
+      "upstreamWithReason": "Upstream error: {{reason}}",
       "network": "Network unreachable"
     }
```

- [ ] **Step 15: 跑全部测试 + typecheck**

```bash
pnpm -r run typecheck
pnpm -r run test
```

Expected: 全部 PASS。如果 gateway 现有 `gateway-core.test.ts` 内 "core 拒连 → 502 upstream_error" 这条 case 断言 message 严格等于 `'upstream_error'`，需要改为 `expect(msg).toMatch(/^upstream_error/)`。grep `'upstream_error'` 找断言位置一并改。

- [ ] **Step 16: Commit**

```bash
git add gateway/src/config.ts gateway/src/server.ts gateway/src/routes/messages.ts gateway/src/routes/sessions.ts gateway/.env.example gateway/test
git add core/src/config.ts core/src/llm core/src/routes/messages.ts core/.env.example core/test
git add client/src/components/ChatDialog.tsx client/src/i18n/locales client/test
git commit -m "feat(gateway,core,client, v6.5): CORE_TIMEOUT_MS + LLM_TIMEOUT_MS 可配 + 错误文案带原因"
```

---

## Phase 4: maxTokens 上限解除 + DB migration

### Task 7: 解除 maxCompletionTokens 上限 + schema_version 4→5（Issue #4）

**Files:**

- Modify: `client/src/components/AgentFormDialog.tsx`（zod 去 max + input 去 max）
- Modify: `client/src/i18n/locales/{zh-CN,en}/translation.json`（placeholder + errors.maxTokensRange）
- Modify: `core/src/routes/agents.ts:17`（zod 去 max）
- Modify: `core/src/routes/agent-item.ts:18`（zod 去 max）
- Modify: `core/src/db/schema.sql:24`（CHECK 去 ≤32000）
- Modify: `core/src/db/index.ts`（SCHEMA_VERSION 4→5 + migration runner）
- Create: `core/src/db/migrations/0005_relax_max_tokens.sql`
- Test: `client/test/components/AgentFormDialog.test.tsx`（100_000 通过）
- Test: `core/test/db/agents.test.ts`（100_000 不报 CHECK 错）
- Test: `core/test/db/schema-version.test.ts`（4→5 迁移）

- [ ] **Step 1: 写失败测试 — client 接受 100_000**

`client/test/components/AgentFormDialog.test.tsx` 加：

```typescript
it('v6.5: maxCompletionTokens 输 100000 → 通过校验（不再触 max 错）', async () => {
  const createSpy = vi.spyOn(agents, 'createAgent').mockResolvedValue({} as never);
  render(
    <AgentFormDialog mode="create" gatewayUrl="http://gw" clientKey="ck"
      onClose={() => {}} onSaved={() => {}} />,
  );
  await userEvent.type(screen.getByLabelText('名称'), 'echo');
  await userEvent.type(screen.getByLabelText('基础 URL'), 'http://x/v1');
  await userEvent.type(screen.getByLabelText('模型'), 'm');
  await userEvent.type(screen.getByLabelText('API Key'), 'sk-test');
  const maxInput = screen.getByLabelText(/单次回复 Tokens|Max Completion Tokens/);
  await userEvent.clear(maxInput);
  await userEvent.type(maxInput, '100000');
  await userEvent.click(screen.getByRole('button', { name: '创建' }));
  await waitFor(() => expect(createSpy).toHaveBeenCalled());
  expect(createSpy.mock.calls[0]![3].maxCompletionTokens).toBe(100_000);
});
```

- [ ] **Step 2: 写失败测试 — core db 接受 100_000 + migration 跑通**

`core/test/db/agents.test.ts` 加：

```typescript
it('v6.5: max_tokens 100000 → 写入成功（CHECK 已放宽）', () => {
  const dao = new AgentsDAO(db);
  const id = randomUUID();
  expect(() =>
    dao.create({
      id,
      name: 'big',
      description: '',
      llm_provider: 'openai-compatible',
      base_url: 'http://x/v1',
      model: 'm',
      max_tokens: 100_000,
      context_window: null,
      reasoning_effort: null,
      api_key: null,
      enabled_api: 0,
      system_prompt: '',
      capabilities: '[]',
      created_at: 't',
      updated_at: 't',
    }),
  ).not.toThrow();
});
```

`core/test/db/schema-version.test.ts` 加：

```typescript
import { describe, it, expect } from 'vitest';
import Database from 'better-sqlite3';
import { tmpdir } from 'node:os';
import { mkdtempSync, rmSync } from 'node:fs';
import { join } from 'node:path';
import { randomUUID } from 'node:crypto';
import { openDatabase } from '../../src/db/index.js';

it('v6.5: 老 DB (schema_version=4) 启动 → migration 升到 5 + max_tokens CHECK 放宽', () => {
  const dir = mkdtempSync(join(tmpdir(), 'core-mig-'));
  const path = join(dir, `${randomUUID()}.db`);
  try {
    // 1) 用 v6.4 时期的 schema 建一个 v4 老库
    const seed = new Database(path);
    seed.pragma('journal_mode = WAL');
    seed.pragma('foreign_keys = ON');
    seed.exec(`
      CREATE TABLE agents (
        id TEXT PRIMARY KEY,
        name TEXT NOT NULL UNIQUE,
        description TEXT NOT NULL DEFAULT '',
        llm_provider TEXT NOT NULL DEFAULT 'openai-compatible',
        base_url TEXT NOT NULL,
        model TEXT NOT NULL,
        max_tokens INTEGER,
        context_window INTEGER,
        reasoning_effort TEXT,
        api_key TEXT,
        enabled_api INTEGER NOT NULL DEFAULT 0,
        system_prompt TEXT NOT NULL DEFAULT '',
        capabilities TEXT NOT NULL DEFAULT '[]',
        created_at TEXT NOT NULL,
        updated_at TEXT NOT NULL,
        CHECK (llm_provider = 'openai-compatible'),
        CHECK (max_tokens IS NULL OR (max_tokens >= 1 AND max_tokens <= 32000)),
        CHECK (length(name) > 0 AND length(name) <= 64)
      );
      CREATE UNIQUE INDEX idx_agents_name ON agents(name);
      CREATE TABLE sessions (id TEXT PRIMARY KEY, agent_id TEXT NOT NULL, client_key TEXT NOT NULL, title TEXT NOT NULL DEFAULT '', created_at TEXT NOT NULL, updated_at TEXT NOT NULL);
      CREATE TABLE messages (id TEXT PRIMARY KEY, session_id TEXT NOT NULL, role TEXT NOT NULL, content TEXT NOT NULL, created_at TEXT NOT NULL);
      CREATE TABLE schema_version (version INTEGER PRIMARY KEY, applied_at TEXT NOT NULL);
      INSERT INTO schema_version (version, applied_at) VALUES (4, 't0');
      -- 老数据：放一行确保 migration 复制时不丢
      INSERT INTO agents (id, name, base_url, model, created_at, updated_at)
        VALUES ('a1', 'echo', 'http://x/v1', 'm', 't0', 't0');
    `);
    seed.close();

    // 2) 调 openDatabase → 触发 4→5 migration
    const db = openDatabase(path);

    // 3) 断言 version 升到 5
    const row = db.prepare('SELECT version FROM schema_version').get() as { version: number };
    expect(row.version).toBe(5);

    // 4) 断言新 CHECK 已生效：插 max_tokens=100_000 不抛
    expect(() =>
      db
        .prepare(
          `INSERT INTO agents (id, name, base_url, model, max_tokens, created_at, updated_at)
         VALUES (?, ?, ?, ?, ?, ?, ?)`,
        )
        .run('a2', 'big', 'http://x/v1', 'm', 100_000, 't1', 't1'),
    ).not.toThrow();

    // 5) 断言老数据仍在
    const existing = db.prepare('SELECT name FROM agents WHERE id=?').get('a1') as { name: string };
    expect(existing.name).toBe('echo');

    db.close();
  } finally {
    rmSync(dir, { recursive: true, force: true });
  }
});
```

- [ ] **Step 3: 跑测试确认失败**

```bash
pnpm --filter @my-ai/client run test -- test/components/AgentFormDialog.test.tsx
pnpm --filter @my-ai/core run test -- test/db
```

Expected: 三条新 case 失败。

- [ ] **Step 4: 改 client/src/components/AgentFormDialog.tsx**

第 26 行：

```diff
   maxCompletionTokens: z
-    .union([z.literal(''), z.coerce.number().int().min(1).max(32000)])
+    .union([z.literal(''), z.coerce.number().int().min(1)])
     .default(''),
```

第 417 行 input：删 `max={32000}` 属性。

- [ ] **Step 5: 改 client/src/i18n/locales/zh-CN/translation.json**

```diff
       "maxCompletionTokens": {
         "label": "单次回复 Tokens（最大输出）",
-        "placeholder": "默认 4096；范围 1..32000"
+        "placeholder": "默认 4096；不限上限（以 upstream LLM 为准）"
       },
       ...
     "errors": {
       ...
-      "maxTokensRange": "maxTokens 必须 1..32000",
+      "maxTokensRange": "maxTokens 必须 ≥1",
```

`en/translation.json` 同：

```diff
       "maxCompletionTokens": {
         "label": "Max Completion Tokens",
-        "placeholder": "Default 4096; range 1..32000"
+        "placeholder": "Default 4096; no upper limit (governed by upstream LLM)"
       },
       ...
-      "maxTokensRange": "maxTokens must be 1..32000",
+      "maxTokensRange": "maxTokens must be ≥1",
```

- [ ] **Step 6: 改 core/src/routes/agents.ts 第 17 行**

```diff
-  maxTokens: z.number().int().min(1).max(32000).nullable().default(null),
+  maxTokens: z.number().int().min(1).nullable().default(null),
```

- [ ] **Step 7: 改 core/src/routes/agent-item.ts 第 18 行**

```diff
-    maxTokens: z.number().int().min(1).max(32000).nullable().optional(),
+    maxTokens: z.number().int().min(1).nullable().optional(),
```

- [ ] **Step 8: 改 core/src/db/schema.sql 第 24 行 + header**

把第 1-5 行 header 注释末尾加一行：

```
-- v6.5 放宽 max_tokens（去 ≤32000）→ schema_version=5。
```

第 24 行：

```diff
-  CHECK (max_tokens IS NULL OR (max_tokens >= 1 AND max_tokens <= 32000)),
+  CHECK (max_tokens IS NULL OR max_tokens >= 1),
```

- [ ] **Step 9: 创建 core/src/db/migrations/0005_relax_max_tokens.sql**

```sql
-- 4→5: 解除 agents.max_tokens 上限（≤32000 → 仅保留 ≥1）。
-- SQLite 不支持 ALTER CONSTRAINT，需表重建。所有字段与 schema_version=4 一致，
-- 仅 max_tokens CHECK 被放宽。
BEGIN TRANSACTION;

CREATE TABLE agents_new (
  id                TEXT    PRIMARY KEY,
  name              TEXT    NOT NULL UNIQUE,
  description       TEXT    NOT NULL DEFAULT '',
  llm_provider      TEXT    NOT NULL DEFAULT 'openai-compatible',
  base_url          TEXT    NOT NULL,
  model             TEXT    NOT NULL,
  max_tokens        INTEGER,
  context_window    INTEGER,
  reasoning_effort  TEXT,
  api_key           TEXT,
  enabled_api       INTEGER NOT NULL DEFAULT 0,
  system_prompt     TEXT    NOT NULL DEFAULT '',
  capabilities      TEXT    NOT NULL DEFAULT '[]',
  created_at        TEXT    NOT NULL,
  updated_at        TEXT    NOT NULL,
  CHECK (llm_provider = 'openai-compatible'),
  CHECK (max_tokens IS NULL OR max_tokens >= 1),
  CHECK (context_window IS NULL OR (context_window >= 1 AND context_window <= 2000000)),
  CHECK (reasoning_effort IS NULL OR reasoning_effort IN ('none', 'minimal', 'low', 'medium', 'high', 'xhigh')),
  CHECK (length(name) > 0 AND length(name) <= 64),
  CHECK (length(description) <= 256),
  CHECK (length(model) > 0 AND length(model) <= 128),
  CHECK (length(base_url) > 0 AND length(base_url) <= 512),
  CHECK (length(system_prompt) <= 8192),
  CHECK (enabled_api IN (0, 1))
);

INSERT INTO agents_new SELECT * FROM agents;
DROP TABLE agents;
ALTER TABLE agents_new RENAME TO agents;

CREATE UNIQUE INDEX IF NOT EXISTS idx_agents_name ON agents(name);

UPDATE schema_version SET version = 5, applied_at = strftime('%Y-%m-%dT%H:%M:%fZ','now');

COMMIT;
```

- [ ] **Step 10: 改 core/src/db/index.ts — bump version + migration runner**

把第 14 行 `SCHEMA_VERSION = 4` 改 5。

在文件顶部 imports 后加：

```typescript
// v6.5: 渐进 migration runner（之前是 loud-fail）。MIGRATIONS[N] 把 N → N+1。
const MIGRATIONS: Record<number, (db: DatabaseType) => void> = {
  4: db => {
    db.exec(loadMigrationSql('0005_relax_max_tokens.sql'));
  },
};

function loadMigrationSql(name: string): string {
  const url = new URL(`./migrations/${name}`, import.meta.url);
  return readFileSync(fileURLToPath(url), 'utf8');
}
```

替换 `openDatabase` 的 "已存在的检查 + loud-fail" 块（行 47-57）为：

```typescript
  } else {
    // 后续启动：检查 version；不符 → 尝试 migration 升级，否则 loud fail
    const row = db.prepare('SELECT version FROM schema_version').get() as
      | { version: number }
      | undefined;
    const cur = row?.version;
    if (cur === undefined) {
      throw new Error('schema_version missing in existing DB');
    }
    if (cur > SCHEMA_VERSION) {
      // 二进制比 DB 旧 → loud fail（降级风险）
      throw new Error(
        `schema version mismatch: DB ${cur} > expected ${SCHEMA_VERSION}; downgrade unsafe`,
      );
    }
    // 顺序应用 cur → SCHEMA_VERSION 的所有 migration
    for (let v = cur; v < SCHEMA_VERSION; v++) {
      const m = MIGRATIONS[v];
      if (!m) {
        throw new Error(`missing migration ${v} → ${v + 1}`);
      }
      m(db);
    }
  }
```

- [ ] **Step 11: 跑全部测试 + typecheck**

```bash
pnpm -r run typecheck
pnpm -r run test
```

Expected: 全部 PASS（包含新加的 client/core 三条 case + 既有 116 + 150 + 118）。如有任何快照变化 update。

- [ ] **Step 12: 手动验证 migration 端到端**

在 dev 模式启动现有 core（带 v4 DB）：

```bash
pnpm --filter @my-ai/core run dev
```

Expected: 启动 log 不报 schema mismatch；用 sqlite3 CLI 验证：

```bash
sqlite3 core/core.db "SELECT version FROM schema_version;"
# → 5
sqlite3 core/core.db "SELECT sql FROM sqlite_master WHERE type='table' AND name='agents';"
# → 输出含 "CHECK (max_tokens IS NULL OR max_tokens >= 1)"，不含 "<= 32000"
```

- [ ] **Step 13: Commit**

```bash
git add client/src/components/AgentFormDialog.tsx client/src/i18n/locales client/test
git add core/src/routes/agents.ts core/src/routes/agent-item.ts core/src/db core/test
git commit -m "feat(client,core, v6.5): 解除 maxCompletionTokens 上限 + DB schema 4→5 migration"
```

---

## 完成验证（全部 phase 跑完后）

- [ ] 跑全套测试 + typecheck + lint：

```bash
pnpm -r run typecheck
pnpm -r run test
pnpm -r run lint
```

Expected: 全部 PASS。

- [ ] 查看 git log 应有 7 个 commit（每 task 一个，按顺序）：

```bash
git log --oneline | head -10
```

- [ ] 按 spec §9 手动验证清单逐条过：

1. **#6** Tauri build → 配对 → kill 进程 → 重启 → 应直接 PAIRED
2. **#2** Ollama qwen2.5:32b + 复杂 prompt → 60s+ 不再 502
3. **#1** 编辑 agent 时手动让 PairKey 失效 → 触发 401 → PairDialog 在 AgentForm 之上可点
4. **#3** LLM 思考 20s+ → 按钮 "生成中… Ns" + spinner + 取消可点
5. **#4** 编辑 agent maxCompletionTokens=100_000 → 保存成功；启动新 core → migration 自动跑通
6. **#5** 编辑老 agent（apiKey=null）→ 必须填 apiKey 才能保存
7. **#7+#8** 切中文 → 主页 / 配对弹窗 / 主题按钮全中文
