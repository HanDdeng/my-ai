# 前端可见的环境变量（VITE_ 前缀才会被注入到 import.meta.env）。
# 网关层地址：前端 fetch / WebSocket 都走这个。
VITE_GATEWAY_URL=http://127.0.0.1:8787
# 客户端 heartbeat 间隔（毫秒），未设 = 默认 5 分钟。
#   调小：更早发现网关断连；代价是网络/CPU 略增。
#   调大：省资源；网关断连时 UI 感知延迟变大。
# VITE_HEARTBEAT_INTERVAL_MS=300000
